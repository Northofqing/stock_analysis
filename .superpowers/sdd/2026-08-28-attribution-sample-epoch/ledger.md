# SDD ledger — plan: docs/superpowers/plans/2026-08-28-attribution-sample-epoch.md

## Controller rulings

- Ruling: Worktree uses only a read-only symlink to the authoritative `client-bundle/market.proto` and an empty ignored `data/` namespace; it never links credentials or a production database — needed to reproduce the baseline; cost if wrong: the build/test environment may not match the production contract.
- Ruling: All Cargo commands use `CARGO_TARGET_DIR=/Users/zhangzhen/Desktop/Quant/stock_analysis/target` because an isolated cold target exhausted disk space; avoid parallel link-heavy commands — needed to keep the isolated worktree usable; cost if wrong: shared build artifacts could obscure an environment-only failure, so task tests must still use exact source revision and isolated TEST_CODE data.
- Ruling: Task 3's phrase “six groups” is non-authoritative because the exact table list defines seven fact/chain families; implement every exact named family from the plan/spec — needed to preserve all append-only chains; cost if wrong: schema/review expectations diverge.
- Ruling: Keep attempt-write authority service-owned or `pub(crate)` unless a later task demonstrates a real public caller need — needed to prevent CLI/monitor from bypassing activation invariants; cost if wrong: a later task may require a narrow API adjustment.
- Ruling: A plan test filter that matches zero tests is not evidence; use the existing concrete read-only session test name and verify the output reports at least one executed test — needed for trustworthy validation; cost if wrong: command text differs from the illustrative plan line.
- Ruling: User explicitly approved continuing with the documented pre-existing `CLAUDE.md` / `monitor_help_isolation` baseline drift; do not modify that plan-external file during attribution implementation — needed to keep task scope honest; cost if wrong: final full-suite evidence will retain one known baseline failure unless the upstream documentation drift is fixed separately.
- Ruling: Task 4's `VerifiedEpochFillSet` omitted the persisted carry vector required by Task 5. Task 5 may narrowly modify `database::attribution_epochs` so the loader-owned verified set carries `Vec<LegacyCarryPosition>` behind read-only accessors, populated only from fully validated retained carry rows — needed to run quarantine without raw SQL/resampling; cost if wrong: Task 5 touches one extra producer-interface file and its focused contract tests.
- Ruling: Task 5 fix round is limited to the two confirmed Spec findings: preserve already-known epoch scope evidence before fee validation, classify fee failures at `TradeEvidence`, and canonicalize multi-missing fee IDs. The review's close-loader duplication and epoch-field mirroring are maintainability observations for final whole-branch review, not safe refactors inside this evidence-binding fix — needed to keep the correction auditable; cost if wrong: duplicated security-sensitive protocol may drift later.
- Final-review observation: active/exact verified trades and the close-only tail enter replay through separate `DatabaseManager`/loader inputs and snapshots. Production CLI currently supplies the same session path, but the public replay constructor does not prove common file identity. Re-assess this cross-input provenance seam in final whole-branch integrity review rather than expanding Task 5 without a design ruling.
- Ruling: Task 6 must preserve the specified main report → epoch binding → binding chain → run-audit order even though the existing report revision has an immediate FK to the run row. It may use deferred FK enforcement inside the same `BEGIN IMMEDIATE` transaction and pre-reserve/validate the unique next run ID before writing, then insert that exact run row last — needed to satisfy both FK integrity and the canonical append order; cost if wrong: run-ID prediction or deferred enforcement bugs could break atomicity, so rollback/concurrency/read-back tests must cover it.
- Ruling: Task 6 may update the report-store schema registry/migration assertions in the same test file to include the epoch tables already introduced by Task 3 plus the two new report-binding tables. The expected registry must describe the complete current schema, not retain a stale hard-coded six-table count — needed for a truthful integrated schema test; cost if wrong: a table omission could be hidden by a mechanically loosened assertion, so exact names remain required.
- Ruling: Task 6's public raw epoch-binding append is an integrity bypass. Fix it on two layers: narrow the raw append/binding construction and `commit_report` authority to crate-internal use (the sealed replay runner remains the sole production caller), and inside the same immediate transaction require an exact retained epoch receipt whose epoch ID, receipt hash, effective date, and legacy-carry manifest all match before any report write. The exclusion manifest remains admitted only through the sealed prepared replay path; direct external construction must be impossible. Legacy is permitted only through the same narrowed authority — needed to prevent fabricated/nonexistent epoch attestations and active-as-Legacy relabelling from public callers; cost if wrong: a future legitimate external report writer would need a new verified capability rather than raw fields.
- Ruling: The Task 6 authority fix must not split full epoch validation from the report write lock. Task 6 may narrowly modify `database::attribution_epochs` to expose a crate-private exact-selector loader/validator over the caller's existing SQLite transaction connection, reusing the one canonical full retained-state implementation. Report commit must invoke it after `BEGIN IMMEDIATE` and before any report write; do not duplicate epoch validation SQL in `attribution_reports` or rely on a four-column reread — needed to eliminate preflight-to-transaction TOCTOU; cost if wrong: the epoch store gains one internal connection-aware seam that final review must ensure cannot write or bypass selector integrity.
- Ruling: Task 7 daily persistence requires an atomic batch seam because the existing `append_daily` accepts one signal family and would otherwise commit a day across multiple transactions. Add a canonical family-sorted batch append in `database::attribution_epochs`: reject duplicate families, validate the complete batch before writes, and in one `BEGIN IMMEDIATE` independently reuse identical family payloads or append changed-family revisions with their chains. Keep the single-family API only as a batch-of-one compatibility wrapper — needed to prevent partial daily persistence; cost if wrong: per-family revisions within one batch may differ when only some payloads change, so the batch receipt/tests must make that explicit and deterministic.
- Ruling: Task 7 compute evidence must be revalidated atomically at persistence time. `EpochDailyAttribution`/window-derived daily payloads carry the active epoch/receipt identity plus the database-verified source highwater/manifests/tip used for scoping. The production batch append accepts that binding and, inside its own `BEGIN IMMEDIATE` before any daily write, uses the canonical same-connection Active selector and verified-fill loader to prove the active receipt and frozen source evidence still match. Any activation change, late source row, prefix/terminal drift, or manifest mismatch fails with zero daily rows/sequences; the raw single-family wrapper remains compatibility-only and is not a production persistence path — needed to prevent committing stale computed attribution after compute/persist TOCTOU; cost if wrong: the daily append API becomes evidence-bearing and requires a narrow read-only same-connection verified loader seam.
- Ruling: Task 7 compute must also obtain Active receipt and verified fills from one read-only database snapshot/connection. Add a narrow store-owned read seam that invokes the canonical same-connection selector and verified-fill implementations together; `performance::attribution` may not perform two independent loads. This prevents selector/source TOCTOU from generating internally mixed daily/window output or downstream markdown before persistence revalidation — needed for fail-closed monitor behavior; cost if wrong: the store exposes one additional composed read capability whose returned receipt/set consistency must be tested under deterministic drift.
- Ruling: Task 7 fix must bind the verified loader's current all-status paper high-water (and explicit current audit high-water) rather than reusing only activation-receipt frozen boundaries. Both values are part of the sealed daily source binding and are compared under the persistence write transaction; a late NotFilled/Invalidated row with unchanged audit tip must fail with zero writes — needed to cover all source rows, not only Filled economics; cost if wrong: otherwise apparently irrelevant status rows could bypass compute/persist snapshot equality.
- Ruling: Task 7 daily output must carry an opaque, privately constructed database authority identity captured from the exact compute connection and checked against the exact persist transaction connection. It must at least bind canonical SQLite main-file identity (path plus device/inode or a stronger existing manager attestation), reject a byte-identical cloned database, remain unavailable to raw callers, and stay out of caller-controlled family payload fields unless represented canonically — needed to prevent cross-database capability replay; cost if wrong: filesystem moves/reopens may need a future explicit re-compute rather than accepting a stale in-memory result.
- Ruling: Task 7's dynamic paper-source evidence must include a canonical all-status manifest over every fully validated current `paper_trades` row in strict ID order, not only `MAX(id)` plus the Filled manifest. Thread that manifest through `VerifiedEpochFillSet`, the sealed daily evidence/canonical family payload, and the protected persistence binding, then compare it under the same write transaction. Cover a valid existing NotFilled/Invalidated mutation and a valid below-high-water gap insertion with zero fact/chain/sequence writes — needed because `paper_trades` is mutable and a high-water alone does not identify its contents; cost if wrong: adding non-economic terminal rows may now invalidate an in-flight daily computation and require recompute.
- Ruling: The prior pathname/device/inode Task 7 authority is insufficient and is superseded. Attribution database authority must be issued by `DatabaseManager` from the exact checked-out SQLite connection's registered descriptor attestation, bind the actual main/WAL/SHM file objects, and remain comparable without exposing caller-mintable fields. Task 7 may narrowly extend `database::mod` and the existing descriptor-attestation machinery plus affected test constructors to register legacy-pool checkouts; it may not accept `PRAGMA database_list` pathname metadata as identity evidence. Add an open-connection rename-and-byte-identical-replacement regression proving detached A evidence cannot persist to B while the originating attested checkout remains internally consistent — needed to close the reproduced connection/path inode mismatch; cost if wrong: legacy pool construction becomes fail-closed on platforms where exact descriptor attestation is unavailable, which must surface as typed attribution unavailability rather than fallback.
- Ruling: Exact-checkout association may not be represented by accepting an arbitrary `SqliteConnection` plus a manager-local predictable TEMP token. Introduce a crate-private attested attribution-checkout wrapper whose private constructor acquires both the pooled connection and its exact `DescriptorSqliteSource` from one `DatabaseManager`; authority validation operates only through that branded wrapper. Make registration tokens source-unique and install update/delete protection as defense in depth, and cover the reproduced manager-B + detached-checkout-A case — needed so a proof from another manager/connection cannot be selected while validating different SQLite state; cost if wrong: Task 7 read/write seams must use the branded checkout rather than generic `get_conn`.
- Ruling: Descriptor attestation is an attribution-only optional capability for the ordinary runtime manager, not a replacement for the global operational pool. `DatabaseManager::init` must retain the existing operational pool for unrelated consumers and may create a separate optional attested attribution pool/source; unsupported/unavailable attestation leaves the operational database usable and makes only Task 7 return typed `attribution_database_authority_unavailable`. The already descriptor-owned amended-selection constructor may safely reuse its attested pool — needed to preserve monitor/trading failure isolation; cost if wrong: one extra small SQLite pool is retained for attribution on supported platforms.
- Ruling: Preserve the descriptor error taxonomy at the Task 7 boundary. Absence or platform unavailability maps to non-retryable typed Unavailable, while a registered token/proof, retained descriptor, namespace, or main/WAL/SHM identity change maps to `attribution_epoch_integrity_failed` / FailedIntegrity and rolls back all writes. Do not string-match errors to decide the class — needed so proven substitution/tamper is never reported as ordinary capability absence; cost if wrong: the database authority error enum/API gains a distinct integrity variant.
- Ruling: The attribution pool must not let r2d2's `test_on_check_out` discard and replace a registered connection after token/proof/fd drift. Build the attribution-only pool without opaque checkout validation, validate immediately inside the branded checkout before returning it, and latch the first live registered-source integrity failure on `DescriptorSqliteSource` so later checkouts/connects fail as typed integrity until manager restart. Normal proof cleanup for already-closed connections may remain in `connect`; only an error observed while validating a live registered checkout/authority is latched. On `pool.get` failure, re-check the typed source latch/namespace before classifying the remaining error as unavailable. The amended-selection constructor must use the same source with a separate attribution pool configured for this behavior rather than cloning a checkout-validating pool — needed to prevent r2d2 self-heal/error erasure; cost if wrong: the source retains one first-failure detail and the selection constructor builds two small pools from one manager.
- Ruling: Both deferred and immediate branded transactions must perform exit authority validation even when the operation returns `Err`; a proven exit integrity failure takes precedence over the operation error, otherwise the original operation error is returned. This remains inside the same transaction closure so every error path rolls back — needed so business/query failures cannot mask concurrent descriptor substitution; cost if wrong: the transaction wrapper must combine two results rather than using early `?`.
- Ruling: Selection proof, descriptor connect, r2d2 live validation, and branded attribution checkout over one `DescriptorSqliteSource` must share the same set-once first-integrity-failure latch. A failure from any path blocks every later path with the identical typed `DescriptorIntegrityFailed`, including an already-checked-out selection connection — needed so the separate selection and attribution pools cannot disagree about a proven source substitution; cost if wrong: all live registered-connection validators must route through one typed core and retain the failure until manager restart.
- Ruling: Task 8 may narrowly extend `database::attribution_epochs` with a path-only cold preview entry because opening `AttributionDatabaseSession::ReadOnly` first can create/wait on WAL sidecars before the existing immutable preview runs. The new entry must reject symlinks/non-files, canonicalize the explicit path, reuse the one existing cold `mode=ro`/immutable snapshot implementation, and never construct a normal pool or write/migrate; the existing manager-owned preview must delegate to the same implementation — needed to satisfy the specified main/WAL/SHM byte-and-mtime immutability for a clean WAL database; cost if wrong: one extra public read-only activation seam exists and must remain path-validation-only rather than becoming a general raw database capability.
- Ruling: Task 8 commit rendering may not cold-reopen the path after `activate_once`, because a legitimate concurrent monitor can retain WAL/SHM and turn an already-durable success into a CLI error. Refactor the shared activation service to produce one domain-verified commit outcome containing the receipt and its renderable frozen projection from the same activation transaction; keep the existing receipt-only API as a wrapper for monitor/other callers. The CLI must not reimplement per-field receipt/projection equality — needed so commit outcome, readback integrity, and presentation cannot diverge after mutation; cost if wrong: the epoch module gains one read-only result type/API and activation internals return a richer private outcome.
- Ruling: `EpochActivationPreview` must carry the calendar authority used to derive its epoch identity and an optional retained receipt identity. A new preview has no receipt; an already-activated preview carries the exact frozen receipt/calendar values. CLI rendering uses these fields directly and represents Preview versus Committed with an explicit state rather than a `bool` plus independent optional hash — needed to avoid contradictory or current-calendar output; cost if wrong: existing preview constructors/tests must initialize and validate two additional evidence fields.
- Ruling: Every successful scheduled/replay/quarter rendering must project the sealed epoch evidence already present in `PreparedAttributionReport`: selector, epoch/receipt/effective date, legacy-carry manifest, remaining quarantine, released/excluded/overlap statistics, exclusion manifest/items, and epoch-scoped fill manifest. Task 8 may add only the missing read-only scoped-manifest accessor to `performance::attribution_replay`; it may not expose constructors or mutable evidence — needed to satisfy design §10 and make selector scope auditable in both JSON and Markdown; cost if wrong: one narrow accessor expands the opaque report's public read surface.
- Ruling: Task 9 may narrowly extend `EpochActivationOutcome` with an immutable `Activated | AlreadyActive` disposition selected inside the same `BEGIN IMMEDIATE` branch that creates or verifies the receipt. Monitor must consume that disposition after the shared full source verification; it may not pre-read `load_selector` to infer the label because a concurrent activation creates a TOCTOU. Keep the existing receipt-only `activate_once` wrapper and all Task 8 projection semantics unchanged — needed for truthful monitor `Activated` versus `Verified` outcomes under concurrency; cost if wrong: one additional read-only outcome field/enum becomes public and all activation branch tests must assert it.
- Ruling: Task 9 activation scheduling must derive one explicit Shanghai `+08:00` timestamp from UTC and use that exact value for the outer guard, activation date, and service call. Main and runtime share the exact `15:35:00..=15:50:00` predicate, while the existing host-local timestamp remains only for unrelated monitor jobs — needed so deployment host timezone cannot suppress activation; cost if wrong: the fixed-offset helper must remain aligned with the checked-in Shanghai calendar policy if that policy ever changes.
- Ruling: Task 10's “all active/exact reads fail” requirement applies through each tampered fact family's existing authority seam, not by expanding `AttributionEpochStore::load_selector` into a cross-module validator. Epoch/carry/attempt/retained-daily structure tamper must reject Active and Exact selector reads; post-highwater paper/audit tamper must reject Active and Exact replay; report-binding tamper must reject the sealed exact-epoch report read/commit path; daily source/chain tamper must reject the active daily compute/persist path (there is no Exact daily API). No seam may fall back to Legacy or an earlier epoch — needed to preserve the spec's module ownership while proving fail-closed behavior; cost if wrong: the matrix is distributed across authority-specific tests instead of one uniform selector test, so its report/review map must explicitly name every case and seam.
- Ruling: Task 10's real E2E coverage gate must assert `coverage_days=2` and `coverage_days_2_below_84`, not the reviewer's proposed value 4. The 8/31–9/1 fills are wholly excluded by the carry quarantine and the spec says exclusions do not enter sample-gate denominators; only the attributable 9/2 buy and 9/3 sell define the fresh sample's coverage — needed to prevent a test from legitimizing quarantine evidence as new sample; cost if wrong: the test binds coverage to the current two-day fresh lifecycle and must change if the fixture later adds another attributable day.
- Final-review observation: `AttestedAttributionCheckout` currently stores an inseparable optional pool/source as paired `Option` fields and duplicates deferred/immediate transaction proof orchestration. Prefer one private capability bundle and a shared proof body when doing so does not complicate transaction lifetimes; these are maintainability observations, not observed correctness failures.
- Final-review observation: Task 7's verified daily append performs repeated full retained-state scans while holding `BEGIN IMMEDIATE`. This is correctness-safe but may increase writer contention as history grows; defer optimization until a single validated snapshot/capability can be threaded without weakening final post-write validation.
- Final-review observation: an otherwise valid completely empty `order_audit` source produces the literal `AUDIT_CHAIN_GENESIS`, while the receipt schema requires a 64-character `order_audit_tip_hash`; activation therefore fails before insertion. Task 8 uses a real canonical audit-chain fixture and does not redefine the Task 4 source model. Final whole-branch review must decide whether zero-audit activation is intentionally unavailable or whether the canonical empty tip/schema contract needs a focused correction.

## Baseline evidence

- Initial worktree build failed because ignored `client-bundle/market.proto` was absent; resolved with the read-only symlink described above.
- A cold worktree-local target failed with `No space left on device`; `cargo clean` removed only that exact worktree target, then the shared target build succeeded.
- Initial full test run had 105 `durable_delivery` failures because the isolated worktree lacked its ignored `data/` namespace. Creating an empty namespace made the representative failed test pass.
- Fresh full baseline result: library tests passed `3015 passed; 0 failed; 7 ignored`; subsequent binary/integration tests reached `tests/monitor_help_isolation.rs` and failed exactly `production_process_rejects_dry_run_before_opening_durable_runtime` (`29 passed; 1 failed`). Root-cause evidence: the test requires an exact annotated `cargo run --bin monitor -- --review ... do NOT set V10_DRY_RUN_PUSH=1` line in `CLAUDE.md`, while current `master` contains only the unannotated command. `git show master:CLAUDE.md` confirms the same mismatch, so this is a pre-existing repository baseline/documentation drift, not an attribution-epoch branch change.

## Preflight task/interface scan

| Task | Own consistency / required output | Shared file or interface overlap | Sequencing ruling |
| --- | --- | --- | --- |
| 1 | `calendar.rs` adds one checked-in-authority-only next-session API with weekend/closure/coverage tests. | Calendar authority is consumed by Task 4 activation and Task 9 runtime. | Land API and tests first; later tasks consume it without alternate date inference. |
| 2 | New pure `attribution_epoch` module owns carry construction, quarantine-until-flat, exclusions, and manifests. | Types/state machine are consumed by Tasks 4, 5, 7, and 10. | Keep pure and storage-free; later storage/replay tasks adapt to this one canonical API. |
| 3 | New `database::attribution_epochs` owns exact schema, triggers, chains, selector reads, daily append, and attempt storage. | `database/mod.rs` and attribution session initialization overlap later Tasks 4, 7, 8, and 9. | Establish schema/read seams before activation; no caller-owned SQL writers. |
| 4 | Activation owns preview/commit, source freeze, idempotency, read-back, and verified fill loading. | Extends Task 3 store; creates integration test file later expanded by Task 10. | Build only after Tasks 1–3; activation derives dates/hash internally and holds `BEGIN IMMEDIATE`. |
| 5 | Replay owns epoch selector admission, quarantine evidence, capability seal, and output statistics. | Modifies `attribution_replay.rs` later touched by Tasks 6 and 10; consumes Tasks 2/4. | Land evidence model before report binding so Task 6 can bind canonical prepared evidence. |
| 6 | Report commit owns immutable report-to-epoch binding and transactional reuse checks. | Modifies `attribution_reports.rs` initially touched by Task 3 and replay touched by Task 5. | Extend existing append transaction; do not create a second independent report writer. |
| 7 | Daily attribution owns active-only read and append-only epoch-bound persistence. | Consumes Task 3/4 store and Task 2 scope; monitor call sites change in Task 9. | Library API first; monitor wiring later. Legacy table remains untouched. |
| 8 | CLI owns `reset-sample` parsing/rendering and selector propagation only. | Consumes Task 4 service and Task 5 replay request; CLI file revisited by Task 10 tests. | No repository SQL or fallback in CLI; preview uses read-only session. |
| 9 | Monitor runtime owns the fixed-window decision adapter and narrow main-loop call. | Consumes Task 4 activation and Task 7 daily APIs; integration isolation test already exists. | Keep runtime thin and independent from trading latches/DB-path discovery. |
| 10 | E2E tests own cross-boundary fixture, tamper matrix, concurrency, and legacy-defect regression. | Intentionally exercises files/interfaces from Tasks 4, 5, 6, 8, and 9. | Treat production-code edits as gap fixes only; review any such hunk against the owning earlier task contract. |
| 11 | Gate task owns BR-255 path/status evidence and fresh Gate C commands. | May format all changed files and records final paths in `docs/business_rules.md`. | Gate C PASS only after every exact fresh command and coverage ratchet pass; offline PR cannot claim freshness. |
| 12 | PR task owns evidence text and external PR/merge operations, no repository implementation. | Depends on all prior commits/checks and repository policy. | Stop for user authority before push/PR/merge external side effects; Gate D remains blocked. |

## Explicit shared-file/interface pairs

| Producer | Consumer | Shared seam | Conflict prevention |
| --- | --- | --- | --- |
| T1 | T4/T9 | `verified_next_a_share_trading_day` and calendar coverage errors | No workday fallback or caller-supplied effective date. |
| T2 | T4/T5/T7/T10 | selector/carry/exclusion types and manifest helpers | One canonical type set in `performance::attribution_epoch`; no duplicated CLI/database enums. |
| T3 | T4/T7/T8/T9 | `AttributionEpochStore`, exact schema, session access modes | Store owns validation/writes; public surface stays narrow. |
| T4 | T5/T8/T9/T10 | receipt, preview, activation, verified scoped fill loader | Service derives completed/effective/calendar from one +08 invoked time. |
| T5 | T6/T8/T10 | `ReplayRequest`, sealed epoch evidence, prepared report | Binding consumes sealed values; selector is never silently rewritten. |
| T6 | T10 | report epoch binding tables and commit transaction | E2E tamper cases use the public/report-store boundary, not direct production shortcuts. |
| T7 | T9/T10 | active daily compute/persist API | Monitor cannot fall back to legacy full-table SQL. |
| T8 | T10 | CLI parser/output and reset service adapter | E2E validates preview byte immutability and commit idempotency. |
| T9 | T10 | runtime window/latch outcome contract | Tests prove failure does not alter trading state or suppress retries. |
| T11 | T12 | verified evidence and BR-255 Gate status | PR description uses measured outputs; no invented PASS/freshness claims. |

## Task results

### Task 1 — verified next trading day

- Base/head: `39ed9fb1708deebed24e836564bcff11c83652b4` → `1529e21c43b61a3632208ff072e3e04db44a6db8`.
- Implementer: DONE; focused tests `2/2`, calendar tests `34/34`; commit `1529e21 feat: resolve next verified trading day`.
- Independent review: Spec compliant; quality Approved; Critical 0, Important 0, Minor 1.
- Non-blocking final-review note: no explicit maximum-`NaiveDate` arithmetic-overflow test, although both increments use checked arithmetic.

### Task 2 — legacy carry and quarantine-until-flat

- Base/head: `1529e21c43b61a3632208ff072e3e04db44a6db8` → `d102ad9dac7bf58613e90f13c0ccd3ebff7c3a80`.
- Initial commit: `434742f feat: scope attribution fills by immutable epoch`; focused state-machine `8/8` and BR-248 `8/8` passed.
- Initial independent review: Critical 1 (no-carry/post-release oversell not rejected), Important 1 (exclusion manifest did not bind canonical occurrence time), Minor 1.
- Fix commit: `d102ad9 fix: validate attribution epoch quantities and manifests`; state-machine `8/8`, BR-248 `8/8` passed.
- Scoped re-review: both findings ADDRESSED; no new Critical/Important breakage. Task approved.
- Follow-up controller check: `cargo fmt --all -- --check` exited 0, so the implementer's earlier calendar-format concern is not reproducible at final Task 2 head.

### Task 3 — immutable epoch/carry/attempt/daily storage

- Base/head: `d102ad9dac7bf58613e90f13c0ccd3ebff7c3a80` → `598846c874540cc077a190d7bdb01b459aaae499`.
- Initial commit `6fe21e7 feat: persist immutable attribution epochs`; epoch `10/10`, concrete read-only session `1/1`, `cargo check --lib` passed.
- Initial review: Critical 2 (Legacy integrity bypass; empty sequence registry/history erasure), Important 5 (attempt outcome binding; exact trigger registry; exact UTC; raw session seam; mutation coverage).
- Fix commit `598846c fix: harden attribution epoch integrity`; epoch `18/18`, read-only `1/1`, check/rustfmt/diff passed.
- Scoped re-review: all actionable Task 3 findings ADDRESSED; no new Critical/Important breakage.
- Final-review observation: `AttributionDatabaseSession::database()` → public `DatabaseManager` remains a pre-existing cross-crate capability used by unchanged replay/benchmark callers. It was not introduced/broadened by Task 3 and cannot be narrowed without coordinated later-task/caller work; re-assess in final whole-branch review.

### Task 4 — atomic activation and frozen source boundary

- Base/head: `598846c874540cc077a190d7bdb01b459aaae499` → `8f96a4b`.
- Initial commit `ea48499 feat: activate attribution epoch atomically`; unit `30/30`, integration `1/1`, check passed.
- Initial review: Critical 3 (RW preview; invalid retry gate bypass; Filled SQL prefilter), Important 2 (terminal time semantics; negative/snapshot coverage).
- Fix round 1 commit `f6c303f fix: harden attribution epoch activation invariants`; unit `37/37`, integration `1/1`, check passed. Scoped re-review closed preview, retry, all-status and coverage findings; terminal cardinality/date remained open.
- Concurrency diagnosis: the custom unit pool skipped production `DatabaseManager::init` WAL bootstrap and ran DELETE journal. Measured transaction times falsified a 5s timeout theory; production-WAL integration passed. The fix changed only the concurrency fixture to production-equivalent WAL and cleanup ordering; no production retry/lock behavior changed.
- Fix round 2 commit `8f96a4b fix: enforce authoritative terminal binding`; unit `39/39`, integration `1/1`, check passed. Cardinality-first terminal selection plus Shanghai quote-date binding independently re-reviewed ADDRESSED with no new breakage.

### Task 5 — epoch-aware replay evidence

- Base/initial head: `8f96a4b` → `32b820a feat: bind replay evidence to attribution epoch`.
- Implementer: replay `48/48`, economic-position `8/8`, database epoch `40/40`, epoch-focused `7/7`; `cargo check --lib`, rustfmt, and diff check passed. Narrow approved DB producer extension exposes only fully validated retained carry from `VerifiedEpochFillSet`.
- Initial independent review: Critical 0; Spec Important 2. Fee validation happened before recording already-known epoch scope evidence and mapped failures to `ReplayStage::Epoch`; multi-missing fee IDs inherited nondeterministic `HashSet::difference()` ordering. Changes requested.
- Non-blocking review observations: duplicated pinned close-read protocol, mirrored epoch binding fields, and the separate verified-trade/close-tail source identity seam remain for final whole-branch review.
- Fix commit `924c0b7 fix: preserve canonical epoch fee failures`; real Active/Exact and Legacy RED regressions reproduced both findings, then focused `3/3`, replay `51/51`, economic-position `8/8`, `cargo check --lib`, rustfmt, and diff check passed.
- Fresh scoped re-review: both findings ADDRESSED; no new Critical/Important breakage; focused regressions independently passed. Task approved at `924c0b7`.

### Task 6 — immutable report-to-epoch binding

- Base/initial head: `924c0b7` → `94a2c1d feat: bind attribution reports to sample epochs`; report `38/38`, replay `51/51`, economic-position `8/8`, lib check/rustfmt/diff passed.
- Initial review: Critical 0, Important 1 (public raw report append could persist fabricated/nonexistent epoch binding), Minor 1 (per-field manifest/exact-schema negative coverage). Deferred-FK ordering, reserved run ID, reuse, rollback, chains, retention, and identities otherwise verified.
- Fix round 1 `11afb96 fix: verify report epoch binding authority`; narrowed raw authority to crate-internal/sealed runner, added full exact-receipt preflight plus transactional four-field reread and negative matrices; report `42/42`, replay `51/51`, economic `8/8` passed.
- Fix round 1 review: original authority finding addressed, but new Important TOCTOU remained because full epoch validation preceded `BEGIN IMMEDIATE` and transaction-side validation trusted only four columns. Golden manifest coverage remained Minor.
- Fix round 2 `8ec7602 fix: validate report epochs inside write transaction`; added crate-private read-only Exact-only same-connection loader reusing the canonical epoch selector core, moved full validation under the report write transaction before all writes, and added deterministic TOCTOU plus literal golden/five-field tests. Epoch `41/41`, report `44/44`, replay `51/51`, economic `8/8`, lib check/rustfmt/diff passed.
- Fresh round-2 scoped review: TOCTOU, loader contract, and golden/mutation coverage all ADDRESSED; Critical 0, Important 0, no authority/write-order/reuse/Task 5 regression. Task approved at `8ec7602`.
- Final-review observations retained: schema initialization remains multi-statement/non-atomic and validation has repeated linear scans, both extending pre-existing report-store patterns rather than observed Task 6 correctness failures.

### Task 7 — active-epoch daily attribution and append-only persistence

- Base/initial head: `8ec7602` → `d2023a0 feat: scope monitor attribution to active epoch`; active-only daily/window compute, one-snapshot verified inputs, evidence-bearing atomic family batches, and append-only persistence were implemented.
- Hardening commits: `11053c5`, `050098b`, `a0e08d2`, and `76b1e05` bound the canonical all-status source manifest, exact registered main/WAL/SHM authority, branded/random-token checkouts, attribution-only failure isolation, typed integrity errors, first-failure latching, and exit attestation on operation errors.
- Round-4 review found one remaining Important: selection proof bypassed the shared source latch, so selection and attribution pools could disagree after proven drift. No other Critical/Important finding remained; broad attribution `213/213` passed.
- Final fix `92d599a fix: share selection descriptor integrity latch` routes selection proof and r2d2 live validation through one typed set-once core. Focused regressions prove both directions: attribution failure blocks a held selection checkout, and selection-time failure blocks later selection/connect/attribution while preserving the first typed error.
- Fresh round-5 review: **Approved**; Critical 0, Important 0, Minor 1 (test-only WAL bootstrap duplication). Verification passed: focused `2/2`, database `21/21`, attribution epochs `64/64`, performance attribution `17/17`, performance attribution epoch `8/8`, `cargo check --lib`, scoped rustfmt, and diff check. Worktree clean at `92d599a` before ignored SDD evidence updates.
- Final-review observations retained: paired optional pool/source capability fields, duplicated deferred/immediate transaction wrappers, repeated full retained-state scans under `BEGIN IMMEDIATE`, and test-fixture WAL bootstrap duplication. None is an observed Task 7 correctness failure.

### Task 8 — reset-sample CLI and epoch selector

- Base/initial head: `92d599a` → `8285c87 feat: expose attribution epoch CLI`; added the seventh `reset-sample` command, strict/default replay selectors, shared activation preview/commit rendering, and the controller-authorized path-only cold preview seam. Initial verification passed CLI `12/12`, manager preview `5/5`, path-only `1/1`, bin check, help, scoped rustfmt/diff.
- The path-only seam rejects symlink/non-file input, canonicalizes/rechecks the resolved regular file, and shares one immutable cold-preview core with the manager-owned API. A clean WAL-header database with no sidecars preserves main bytes/mtime and never creates WAL/SHM.
- Initial review: Critical 0; Spec Important 3 (replay output omitted epoch evidence; existing preview lost frozen receipt/calendar; post-commit cold preview could turn a durable success into Unavailable under legitimate live WAL); Standards Important 1/Minor 1 (CLI duplicated domain readback equality and encoded render state as `bool` + optional hash).
- Fix `c80c9d6 fix: render verified attribution epoch evidence` added a domain-owned rich activation outcome constructed/validated inside the activation transaction while retaining the receipt-only wrapper, carried frozen calendar/optional receipt in preview, eliminated post-commit cold reopen, used an explicit Preview/Committed render state, and projected complete sealed epoch evidence in JSON/Markdown. `performance::attribution_replay` gained only the missing read-only scoped-fill-manifest accessor.
- Fresh round-2 review: **Approved**; Critical 0, Important 0, Minor 0. Verification passed: focused render/frozen-preview/live-WAL `3/3`, CLI `15/15`, attribution epochs `65/65`, replay `51/51`, bin/lib check, real help, scoped rustfmt, diff and forbidden SQL/fallback scans. Worktree clean at `c80c9d6`.
- Final-review observation retained: the empty `order_audit` genesis/schema length mismatch remains deliberately unfixed and must be classified during whole-branch review.

### Task 9 — monitor activation and active-only attribution

- Base/initial head: `c80c9d6` → `eea5ca4 feat: activate attribution epoch from monitor`; added the thin runtime adapter, safe-window activation with success-only latch/retry outcomes, transaction-owned `Activated | AlreadyActive` disposition, and replaced the old monitor attribution path with active-epoch compute/persist without fake push on failure.
- Initial review: Critical 0, Important 1, Minor 3. The sole blocker was host-local `Local::now()` in the outer activation guard, which could miss the Shanghai window on non-`+08:00` deployments. Full source verification, atomic disposition, retry/latch behavior, no-fake-push path, and trading-state isolation otherwise passed.
- Fix `17b6e65 fix: schedule attribution epoch in Shanghai time` converts a single UTC instant through checked fixed `+08:00`, shares the exact `15:35:00..=15:50:00` predicate between main and runtime, and passes the same guarded timestamp into the service. Unrelated host-local monitor jobs and loop/trading latches remain unchanged.
- Fresh round-2 review: **Approved**; Critical 0, Important 0, Minor 0. Verification passed: runtime `8/8`, commit/retry `1/1`, concurrent disposition `1/1`, `cargo check --bin monitor`, `cargo build --bin monitor`, scoped rustfmt and diff check. Help isolation remained exactly the accepted baseline `29 passed / 1 failed` from unchanged `CLAUDE.md` documentation drift. Worktree clean at `17b6e65` before ignored SDD evidence updates.

### Task 10 — end-to-end legacy-defect, boundary, and tamper regression

- Base/initial head: `17b6e65` → `0d8ebe7 test: cover attribution epoch reset end to end`; test-only changes add the real legacy `510/520` same-day T+1 fixture, carry `300`, complete `530/540/550` quarantine-to-flat sequence, attributable `560/570` lifecycle, preview/commit source immutability, sealed Active/Exact replay/report binding, concurrency/retry, and authority-owned retained/source tamper matrices.
- Final initial verification passed: integration `3/3`, CLI `15/15`, monitor attribution `8/8`, replay `53/53`, scoped rustfmt, and diff check. The 12 tamper targets are mapped to selector, replay, report-binding, or active-daily authority seams per the Task 10 ruling; production behavior and APIs were unchanged.
- Initial independent review: Critical 0, Important 1, Minor 0. The real E2E asserted the `<200` closed-cycle reason but did not independently bind the `<84` coverage-days reason.
- Task 10 fix round 1/5: the reviewer's proposed four coverage days reproduced RED as actual `Some(2)` because 8/31–9/1 are excluded quarantine facts. Controller ruling preserved production semantics; fix `2ca18c5 test: assert attribution epoch coverage gate` asserts `coverage_days=Some(2)` and `coverage_days_2_below_84` while retaining `closed_cycles_1_below_200`. Focused `1/1` and full replay `53/53` passed.
- Scoped fix re-review: **ADDRESSED / Approved**; Critical 0, Important 0, Minor 0, no out-of-scope blocker. Task 10 complete (commits `17b6e65..2ca18c5`, review clean).

### Task 11 — BR-255 Gate C evidence

- BR-255 now lists the monitor epoch runtime, immutable report-binding store, and integration-test active paths. Gate A/B are PASS from the completed Task 1–10 independent evidence; Gate C is **blocked** and Gate D / production freshness remains blocked.
- Fresh strict Clippy passed with `-D warnings`; release monitor and `strategy_attribution` build passed. Fresh workspace tests exited 101 only because `monitor_help_isolation` was `29 passed / 1 failed`, the accepted unchanged `CLAUDE.md` production-dry-run documentation baseline. Fresh llvm-cov repeated that same failure, did not produce JSON, and the PR threshold tool therefore exited 2; LCOV was exported only after creating the output directory.
- Offline PR compliance exited 1. Read-only `git ls-tree` and `git diff master 2ca18c5` prove both `tools/one_shot/_timeout_lib.sh` and `tools/compliance/lib/check_br174_legacy_callers.sh` are absent in master and in Task 11 base; `backfill_daily.sh` is the same `100755` blob in both refs. These are repository-baseline missing-helper failures, not branch regressions or executable-bit drift.
- Exact forbidden scan returned 29 matches, all `#[cfg(test)]`: 25 attribution tamper/rejection fixtures and 4 test DB cleanup guards. `git diff 2ca18c5` has no Task 11 matching line; 14 fixtures predate Task 11 on this branch and 15 are already on master. No production reset/fallback path was found or altered. Full command output, locations, and classifications are in `task-11-report.md`.
- Tracked evidence commit: `10273ea chore: record attribution epoch gate evidence`; final worktree is clean and the `master...HEAD` scope contains only the 16 named attribution-epoch implementation/design/rule paths.
- Task 11 review-fix round 1: reviewer found the Gate-only Clippy box conversion changed the plan-fixed public `ResolvedAttributionEpoch::Epoch(AttributionEpochReceipt)` payload. TDD RED restored the direct public comparison in the integration seam; Box head failed it with E0308 (`expected Box<AttributionEpochReceipt>, found AttributionEpochReceipt`). Fix `5031dbf fix: preserve attribution epoch selector api` restores the direct receipt payload and uses only `#[expect(clippy::large_enum_variant, reason = "BR-255 public resolved-selector payload is a fixed API")]`. Fresh fmt check, strict Clippy, epoch-store `65/65`, replay `53/53`, and integration `3/3` passed. Final-candidate offline PR compliance was rerun: exit 1, `rules: 247, warnings: 162`, with the same two already-recorded missing-helper baseline blockers; freshness remains NOT RUN. The design-header-status Minor is explicitly deferred as outside this Important-only repair.
- Task 11 fix round 2: Phase 1–3 deterministically proved an autocommit snapshot-mixing false integrity failure in post-commit attempt-chain validation. Fix pins the complete query-only retained-state/receipt/source-prefix read-back in one deferred transaction, with a thread-local test-only regression hook; no validation/retry/write-rule change. Controller exact regression session `17368` passed `1/1`; controller optimized stress session `69493` passed `30/30`. Fresh final checks: fmt/diff, strict Clippy, epoch `66/66`, integration `3/3`, replay `53/53`, and release monitor/strategy_attribution all passed. PR compliance remains exit 1 solely on the same two pre-existing missing helpers; business-rule subcheck `247/162`, freshness NOT RUN. Gate C/D remain blocked honestly.
- Task 11 final read-back-authority round: RED proved the path-only post-commit reopen accepted a byte-identical replacement database B as success. Candidate A (checkpoint plus `mode=ro&immutable=1`) was rejected by evidence: a live read-only WAL transaction changed only SHM bytes, violating preview immutability, and on macOS VFS the first immutable reader produced one exclusive main-fd delta while the second produced zero, so repeated connection authority could not be proven without fallback. Candidate B binds the snapshot directly to the still-live branded writer after its `synchronous=FULL` commit: writer main/WAL/SHM authority is checked before serialization, after `sqlite3_serialize`, and after the detached verification; a new `:memory:` connection receives the readonly-deserialized bytes, enforces `query_only`, and runs complete retained-state/receipt/source-prefix validation in one fresh deferred transaction. Serialized images require exact `SQLite format 3\0`, length at least 100, and exact `(1,1)` or `(2,2)` journal versions; only header bytes 18/19 are copied from WAL `(2,2)` to rollback `(1,1)` so the detached reader needs no WAL route, while all business pages remain exact. Focused GREEN evidence: replacement fail-closed `1/1`; new readonly-deserialized connection/read/refuse-write `1/1`; concurrent-writer frozen snapshot `1/1`; authority-exit-over-operation-error `1/1`; header normalization/mixed-version fail-closed `1/1`; preview+commit+idempotent `1/1`; cold preview DB/WAL/SHM immutability `1/1`; real AppendOnly session commit `1/1`. Gate C/D status is unchanged by this correctness round.
- Task 11 replay single-snapshot round: RED proved `AttributionReplayRunner::new(manager_a, loader_b)` could combine manager A's verified epoch/fills with loader B's close tail. Active/Exact now obtain selector, receipt, verified fills, scoped fee evidence, and exact-key closes from one `DatabaseManager`-owned deferred transaction; AppendOnly keeps descriptor-attested branded authority, while ReadOnly captures exact before/after main/WAL/SHM filesystem evidence without opening the source through SQLite, materializes the captured main plus nonempty WAL in a private `0700` directory, requires a complete checkpoint (`busy=0`, non-negative equal log/checkpointed frames), switches the private copy to DELETE, and serves it through a dedicated `mode=ro&immutable=1`, `query_only` pool. The independent loader remains only for Legacy replay and cannot contribute Active/Exact closes. Focused GREEN evidence: CLI immutable Active failure `1/1`; explicit ReadOnly/AppendOnly session contract `1/1`; cross-database fail-closed regression `1/1`; epoch-filter group `9/9`; Active/Exact group `2/2`; existing successful sealed Legacy fixture `1/1`; scoped rustfmt and diff checks passed. Gate C/D status is unchanged by this correctness round.
- Task 11 replay authority review-fix round: independent review reported Critical 0, Important 2, Minor 2. The Important findings were a production ordinary-pool fallback when `DatabaseManager` retained neither attested nor detached-readonly attribution authority, and untyped ReadOnly snapshot errors that conflated source availability with captured-evidence integrity. The Minor findings requested exact-key close-reader empty/401-key coverage and ownership of SQLite's exact `snapshot.db-journal` cleanup artifact with a redacted Drop warning. A real public-runner RED temporarily restored the old test fallback and returned `attribution_epoch_unavailable` instead of the required `attribution_database_authority_unavailable`. The fix makes no-capability production reads return typed `DescriptorAttestationUnavailable`, with ordinary-pool access available only through an explicit `#[cfg(test)]` fixture capability; Legacy remains loader-backed and succeeds without that authority. ReadOnly capture/materialization now returns typed `Unavailable { detail, retryable }` or `Integrity { detail }`, derives retryability only from explicit I/O `ErrorKind`, maps source/temp I/O to availability, and maps non-files, main/WAL/SHM presence or identity/mtime/length/byte drift, corrupt/incomplete WAL materialization, non-DELETE normalization, and retained nonempty sidecars to snapshot integrity. Cleanup owns only main, WAL, SHM, and rollback journal; Drop emits no path or content detail. Focused GREEN evidence: public authority/Legacy regression `1/1`; corrupt nonempty WAL `1/1`; ordinary missing-source typed availability `1/1`; explicit ReadOnly/AppendOnly session `1/1`; rollback-journal cleanup `1/1`; empty plus 401-key close reads `2/2`; cross-database replay `1/1`; epoch filters `9/9`; Active/Exact replay `2/2`; production `cargo check --lib`, scoped rustfmt, and diff checks passed. Gate C/D status is unchanged.
