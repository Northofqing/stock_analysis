# P-02 冻结业务准备：实施记录

日期：2026-09-08。状态：**本批已完成，源码`eeb2ddc`、测试修复`74954fe`；修复后51项测试、静态检查和限定复审通过**。仅关闭冻结业务准备任务，不代表完整W17或P-02迁移上线。

[计划](../superpowers/plans/2026-09-08-auction-volume-frozen-preparation.md)已本地提交617f37f，原始BASE0d84d85。前置[同批修复](implementation-auction-volume-results-2026-09-08.md)已解决双次采集与通知集合错配；本任务不重复该工作，也不把前批13项通过作为新代码证据。

## 真实接线核对发现

- `push_templates.rs:199`横幅渲染仍读取外部动态说明，`:222`的then_some会立即调用账户说明函数，即使账户完整。`:68`沿用户摘要接口可能读库，`:230`又单独读一次全局估值说明。只传同一个BannerCtx并不能保证两次渲染来自同一次完整观察。
- `push_templates.rs:6020`的入池记录包含逐票price和metric_json；部分值不在T11正文中。`:6044`通知code集合也是独立业务提案。仅比较渲染文本不能证明入池与通知集合一致。
- `market_analyzer/limit_up.rs:186`将volume_ratio设为None，`:303`丢弃已有limit-pool/name-shard证据后只返回Vec；`market_analyzer/mod.rs:94`未再补量比。P02准备函数要求存在有限正量比，因此当前此来源不能产生可发送P02快照。已有正例使用受控输入，不证明真实上游提供了量比。这是源码事实，不是对当天生产发送状况的观察。

## 实际实现与调用证据

以下行号对应源码`eeb2ddc`的`src/bin/monitor/push_templates.rs`；上一节是原始BASE故障位置，不混用两个版本。

| 实现点 | 实际行为与证据 |
| --- | --- |
| 一次横幅捕获 | `:79`外部说明读取接口、`:84`真实适配器仍调用既有读取函数；`:234`的BannerCtx实现每次只读一次收盘估值说明，账户完整或已有该说明时不读用户账户摘要。缺失数据不显示成0。 |
| 文案只有一套 | `:96`共享账户说明算法，`:109`保留非横幅调用者；`:198`私有冻结横幅只读返回已保存字节。捕获之后重复渲染不再访问外部说明。 |
| T-11共享渲染 | `:1036`原公开入口保留签名并委托`:1047`纯渲染实现；P-02也调用这同一个实现，不另造测试专用文案。 |
| 完整待执行结果 | `:6064`保存精确消息、有序逐票记录及通知代码集合；`:6115`从同一选中快照一次生成。记录包含代码、名称、价格、精确指标JSON、`P-02`种类与`preopen`来源。 |
| 比较不只看文案 | `:6095`比较正文、记录数量/顺序/全部字段、价格原始位值和代码集合；因此正文四舍五入后相同也能发现原指标差异。冻结横幅与提案的Debug只显示长度/数量。 |
| 真实发送链消费 | `:6172`先准备再发送保存的消息；发送成功后执行保存的记录，全部成功才应用保存的通知集合。`:6215`实际每日入口仍使用原注册元组、发送与记录通道。 |

原过滤、稳定排序、时间窗、共享原始列表和发送通道均保留；本批源码差异没有修改main、上游Gateway或持仓检测。发送失败不入池；缺横幅或任一记录失败都不推进通知集合。第二条记录失败时，第一条既有写入不会回滚，也没有新增自动重发或跨记录原子事务；完整恢复仍属Unit迁移。

采用纯业务准备interface，是为了后续影子执行能精确比较文案之外的业务结果。本批不构造Accepted/Completed，不引入默认认证、自动重发或新provider来源；真实量比合同和来源证据保留仍需后续工程。

## 验收与完整范围

首条真实Banner读取seam反例51886：编译成功2m07s，0通过/1失败，完整账户仍读摘要1次而预期0次；既有文案字节断言通过。最小改为按需闭包后76982：编译12.02s，1通过/0失败/724 filtered，期望未变。

第二条27156复现同次估值说明读取2次而应1次，原三行文案通过。实现一次捕获与私有只读CapturedBanner后64222合跑两条：编译24.95s，2通过/0失败/724 filtered，0.00s；完整账户摘要0次、估值说明1次及原字节期望全部通过。代理补报该中间等待期间已写后续草稿，因此这次只算两条读取行为证据，不作为完整源码冻结证明；未通过回退或重跑旧测试重建过程。

完整源码明确冻结后的45141首轮合批：**51通过/0失败/0忽略/680 filtered**，编译25.85s、运行0.02s。覆盖7项新增、实际dispatcher精确消费、失败不推进、纯横幅、T01–12、blocking_market_data及旧有限正值约束。7项新增具体为4个捕获用例、2个精确提案/比较用例、1个T11共享黄金文案用例；价格变化而正文相同、精确指标变化而显示相同均实际通过。通知集合比较用例从真实准备结果出发，仅在测试内部改变私有集合、保持正文/记录相同以单独验证相等性，没有新增生产修改入口。

25202首轮bin Clippy：exit0，14.84s；完整168行JSON、0坏行、0错误。165项既有诊断分列为lib163项和未改bin2项（`attribution_epoch_runtime.rs:39`、`news_ai_shadow.rs:402`），本批改动文件零诊断。测试编译的84项lib warning与Clippy计数不是同一统计口径。定向rustfmt、diff及冻结RFC输入检查分别通过，未修改无关格式或用allow隐藏诊断。

旧测试文件有固定test_data/test.db写入和可选真实sink端到端用例，因此本批采用已核对的精确前缀及纯函数，不执行那些路径。真实dispatcher的测试日志显式隔离到`/private/tmp/p02-frozen-dispatcher.BLBDjv`，不继承生产/默认目录。未覆盖的端到端和生产运行不声称通过。

独立审查固定原始`0d84d85..eeb2ddc`（计划及实现两个提交）：生产主路径未发现错误，提出1项Important——同批消费测试用新准备函数相等性替换了两条旧独立预期，违反不得删除旧断言的要求。`74954fe`仅恢复原消息literal及固定通知集合的5行断言，保留新增检查，生产逻辑未改，也未伪造新的行为RED。

修复后93572再次完整合批：**51通过/0失败/0忽略/680 filtered**，编译26.56s、运行0.03s；67668再次bin Clippy：exit0、34.26s、完整168行JSON、163旧lib＋2旧bin诊断、本批目标及error均为空。定向fmt、diff与冻结RFC输入检查再通过。限定`eeb2ddc..74954fe`复审确认I1 **ADDRESSED**，无新增Critical/Important，结论 **Approved**；既有warning留最终全分支核对，不扩本轮。

父线完整测试/静态输出、行为矩阵及审查记录位于`.superpowers/sdd/2026-09-08-auction-volume-frozen-preparation/`的`task-1-validation.md`、`task-1-review.md`及其引用原始输出。以上都是开发与隔离测试证据，不是部署或真实接收证明。

尚未完成：来源量比/证据合同、受约束context/projector工厂、真正新旧业务适配、完整业务提案进入shadow比较、全局八类及日志效果纳管、typed authoritative channel与业务最终化/持久游标、逐Unit六门禁和生产批准。W15/W16/W18/W19其余/W20/W21及全部52Unit目标保持不变。

量比来源的进一步只读核对见[竞价量比与来源证据缺口](auction-source-evidence-gaps-2026-09-08.md)。现行BR-213/BR-220明确禁止跨批补量比；不能将其他接口的同名字段直接补入旧投影。真实来源合同及相应产品/提供方事实尚未明确，但保留原始证据的工程可以先行。

本批不启动、监控、替换或重启生产monitor，不操作真实数据库/provider/sink/owner或部署。
