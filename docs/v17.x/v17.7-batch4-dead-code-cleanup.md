# v17.7 — 第四批推送清理 (6 项全 0-caller 直接删除) + v14_adapter 同步

**状态**: 设计中 (待审批)
**日期**: 2026-07-15
**作者**: Claude (MiniMax-M3)
**前置版本**: v17.4 / v17.5 / v17.6
**后续**: v17.8 (5+ 项交易类, 估计 100% 覆盖完成)
**对应 v17.4 §13.7 启动顺序**: v17.7 行
**用户决策 (2026-07-15)**: 选项 A — 6 项全部直接删除

---

## 0. 一句话定位

v17.7 = **本批 6 项 (Announcement / PolicyHit / EarningsBeat / EarningsMiss / AnalystUpgrade / MarketActionAlert) 全部 0 caller**, 与 v17.5/v17.6 不同,**无活跃迁移**。v17.7 是**纯清理批**:

1. **6 项 enum 删除** (与 v17.5/v17.6 一致的"已废弃"处理模式)
2. **同步清理 v14_adapter.rs 映射** (否则编译报 dead-code)
3. **测试用例更新** (notify.rs:1801/1824 用了 Announcement)

> **本批特殊处**: 6 项是 v14 设计期规划,但从未接真实数据源。**不是 v17.x 该批实施的生产迁移**,而是 v14 历史债务。

---

## 1. 背景

### 1.1 用户 G2 决定 (v17.7, 2026-07-15)

用户在确认 v17.6 spec 后,选 G2 "继续 v17.7"。但 grep 实证揭示 6 项全 0 caller,经 AskUserQuestion 用户选 A:**全部直接删除**。

### 1.2 代码实证: 6 项的引用全景

| PushKind | enum 定义 | PushLevel 映射 | dedup 间隔 | 中文标签 | 实际触发 | v14 映射 | 测试 |
|----------|---------|---------------|----------|---------|---------|---------|------|
| Announcement | ✅ notify.rs:29 | ✅ notify.rs:169 Important | — | ✅ notify.rs:327 | ❌ 0 callsite | ✅ v14_adapter:239 | ✅ 1801, 1824 |
| PolicyHit | ✅ notify.rs:144 | ✅ 201 Important | ✅ 298 (86_400s) | ✅ 383 | ❌ 0 callsite | ✅ v14_adapter:307 | — |
| EarningsBeat | ✅ notify.rs:146 | ✅ 202 Important | ✅ 299 (43_200s) | ✅ 384 | ❌ 0 callsite | ✅ v14_adapter:308 | — |
| EarningsMiss | ✅ notify.rs:148 | ✅ 203 Important | ✅ 300 (43_200s) | ✅ 385 | ❌ 0 callsite | ✅ v14_adapter:309 | — |
| AnalystUpgrade | ✅ notify.rs:150 | ✅ 204 Important | ✅ 301 (86_400s) | ✅ 386 | ❌ 0 callsite | ✅ v14_adapter:310 | — |
| MarketActionAlert | ✅ notify.rs:152 | ✅ 206 Emergency | ✅ 302 (60s) | ✅ 387 | ❌ 0 callsite | ✅ v14_adapter:313 | — |

**结论**: 6 项全部只被**配置/标签/映射表**引用,无 `push_governor(...)` 实际触发。

### 1.3 与 v17.5/v17.6 对比 (再次确认 0-caller 模式)

| v17.x | 总项 | 活跃 | 0-caller | 主要工作 |
|-------|------|------|---------|---------|
| v17.5 | 17 | 10 | 7 | 10 迁移 + BR-021 |
| v17.6 | 7 | 4 | 3 | 4 迁移 + DailyReport 子段 |
| **v17.7** | **6** | **0** | **6** | **纯清理** |

### 1.4 v17.5/v17.6 清理模式复用

| 步骤 | v17.5 | v17.6 | v17.7 |
|------|-------|-------|-------|
| enum 直接删除 | ✅ 7 | ✅ 3 | ✅ 6 |
| v14_adapter.rs 同步 | ❌ 旧 enum 未在 adapter 引用 | ❌ 旧 enum 未在 adapter 引用 | ✅ **必做** (6 项都在 adapter) |
| env var 回滚 | ✅ V175_LEGACY | ✅ V176_LEGACY | ✅ V177_LEGACY |
| 测试更新 | — | — | ✅ 1801/1824 (用 Announcement) |

---

## 2. 目标 (6 项全清理)

### 2.1 enum 删除清单

```diff
 pub enum PushKind {
-    Announcement,
     // ... 删 1 项
+    // Announcement 移到 docs/v15.x/dead-pushkinds.md 历史归档
 }
```

完整 6 项: Announcement / PolicyHit / EarningsBeat / EarningsMiss / AnalystUpgrade / MarketActionAlert

### 2.2 v14_adapter.rs 同步清理

```diff
 // src/bin/monitor/v14_adapter.rs
-    PushKind::Announcement => (NewsCatalyst, "announcement", Severity::High),
+    // Announcement 已废弃 (v17.7), 数据源未接, 历史归档
-    PushKind::PolicyHit => (SignalSource::Policy, "policy_hit", Severity::High),
-    PushKind::EarningsBeat => (SignalSource::Earnings, "earnings_beat", Severity::High),
-    PushKind::EarningsMiss => (SignalSource::Earnings, "earnings_miss", Severity::High),
-    PushKind::AnalystUpgrade => { ... },
-    PushKind::MarketActionAlert => { ... },
+    // 上述 5 项 v17.7 删除
```

### 2.3 测试更新

```diff
 // src/bin/monitor/notify.rs:1801
-    PushKind::Announcement,
+    // v17.7: Announcement 已删除, 测试改用其他活跃 PushKind (e.g. NewsFlashCritical)

 // src/bin/monitor/notify.rs:1824
-    PushKind::Announcement,
+    // 同上
```

### 2.4 历史归档

```rust
// docs/v15.x/dead-pushkinds.md (扩展,本次新增 6 项)
# Dead PushKinds (历史归档)

## v14 设计 → v17.7 删除 (从未接数据源)
| PushKind | 设计目的 | 删除版本 | 删除原因 |
|----------|---------|---------|---------|
| Announcement | 公告催化 | v17.7 | 数据源未接 |
| PolicyHit | 政策催化 | v17.7 | 数据源未接 |
| EarningsBeat | 业绩超预期 | v17.7 | 数据源未接 (broker) |
| EarningsMiss | 业绩低于预期 | v17.7 | 数据源未接 (broker) |
| AnalystUpgrade | 卖方评级上调 | v17.7 | 数据源未接 (Wind/Choice) |
| MarketActionAlert | 实盘异常 | v17.7 | 数据源未接 (异动检测) |

## v17.5 删除 (沿用 v13.10.1)
AuctionRepush, OptimalClose, VolumeWatchlist, VolumeRealTrade, CandidateTriggered, CandidateInvalidated, VirtualWatch

## v17.6 删除
FactorIC, SectorTier, CapitalVerify

## v17.4 删除 (✅ 已有)
SectorTop + IpoListingApproval / IpoProspectus / IpoCatalyst
```

---

## 3. 非目标 (本期不做)

- ❌ 接外部数据源 (Wind/Choice/Jin10 等) 重新启用 6 项
- ❌ LLM 摘要 (保留规则模板)
- ❌ MarketActionAlert 用现有行情派生 (用户选 A = 全删)
- ❌ 历史 PushKind 文档自动生成 (手动维护 dead-pushkinds.md)

---

## 4. 架构 (简洁)

### 4.1 文件布局 (3 个文件)

```
src/
├── bin/monitor/notify.rs             # MOD: enum -6, PushLevel match -6, dedup -6, label -6, tests -2
├── bin/monitor/v14_adapter.rs        # MOD: mapping table -6
└── docs/v15.x/dead-pushkinds.md      # MOD: 新增 6 项归档 (已有 v17.5/v17.6 项)

docs/v17.x/v17.7-batch4-dead-code-cleanup.md   # 本文档
```

无新增模块,无新增 EventKind,无 L1 EventBus 集成。

### 4.2 删除前影响面分析

```
notify.rs 改动:
  line 29:    Announcement 删除           (1 行)
  line 144:   PolicyHit 删除              (1 行)
  line 146:   EarningsBeat 删除           (1 行)
  line 148:   EarningsMiss 删除           (1 行)
  line 150:   AnalystUpgrade 删除         (1 行)
  line 152:   MarketActionAlert 删除      (1 行)
  line 169:   Announcement match -1       (1 行)
  line 201-204: PolicyHit/Earnings*/Analyst match -4     (4 行)
  line 206:   MarketActionAlert match -1  (1 行)
  line 298-302: dedup 间隔 match -5       (5 行)
  line 327:   Announcement label -1       (1 行)
  line 383-387: 5 label -5                (5 行)
  line 1801:  测试用例改 PushKind         (1 行)
  line 1824:  测试用例改 PushKind         (1 行)

v14_adapter.rs 改动:
  line 239:   Announcement mapping -1
  line 307-313: 5 mapping -5

dead-pushkinds.md:
  新增 6 行表格 (v17.7 段)
```

总改动: ~36 行删除, ~6 行新增 (dead-pushkinds.md), 总净 -30 行。

---

## 5. 设计 — 3 步清理

### 5.1 Step 1: notify.rs enum + match 清理

```rust
// src/bin/monitor/notify.rs (MOD)

pub enum PushKind {
    // 删除 (v17.7):
    // Announcement, PolicyHit, EarningsBeat, EarningsMiss,
    // AnalystUpgrade, MarketActionAlert,
    // → 历史归档到 docs/v15.x/dead-pushkinds.md

    // 保留 (共 42 个活跃变体):
    AuctionPulse,              // v17.5
    LimitBoardUpdate,          // v17.5
    CandidateBoardUpdate,      // v17.5
    HoldingPlanChange,         // v17.5
    T0AdviceUpdate,            // v17.5
    ForbiddenOpsAlert,         // v17.5
    VirtualTradeExecuted,      // v17.5
    CloseCallAuction,          // v17.5
    AccountModeChanged,        // v17.5
    DataModeChanged,           // v17.5
    DailyReportUpdate,         // v17.6
    EventCalendarUpdate,       // v17.6
    FundInflowAlert,           // v17.6
    TurnoverTopUpdate,         // v17.6
    // ... 28 from v17.4 + L1 基础 (~16) + 保留 ~4
}
```

### 5.2 Step 2: v14_adapter.rs 映射清理

```rust
// src/bin/monitor/v14_adapter.rs (MOD)

impl PushKindV14Adapter {
    fn to_signal_source(kind: PushKind) -> SignalSource {
        match kind {
            // v17.7 删除 6 项, 历史归档见 dead-pushkinds.md
            // PushKind::Announcement => (NewsCatalyst, "announcement", Severity::High),
            // PushKind::PolicyHit => (SignalSource::Policy, "policy_hit", Severity::High),
            // PushKind::EarningsBeat => ...
            // ... 等
            _ => (SignalSource::Unknown, "legacy", Severity::Low),
        }
    }
}
```

### 5.3 Step 3: env var 回滚 + 历史归档

```rust
// v17.7 删除带 env var 回滚 (与 v17.5/v17.6 同模式)
#[cfg(not(feature = "v17_7_legacy_kinds"))]
pub enum PushKind {
    // ... 42 个活跃变体 (无 6 项已废弃)
}

#[cfg(feature = "v17_7_legacy_kinds")]
pub enum PushKind {
    // ... 42 个 + 6 个 legacy 变体 (供回滚测试用)
}

// 运行期 env var 回滚 (无需重新编译)
pub fn is_legacy_kinds_enabled() -> bool {
    std::env::var("STOCK_ANALYSIS_V177_LEGACY_KINDS")
        .map(|v| v == "1")
        .unwrap_or(false)
}
```

---

## 6. toml 配置 (新增)

```toml
# config/monitor.toml (新增段落, 空表占位)

[v17_7_legacy_kinds]
# 6 项 (Announcement/PolicyHit/Earnings*/Analyst/MarketAction) 已直接删除
# 回滚路径: env var STOCK_ANALYSIS_V177_LEGACY_KINDS=1
# 注: 仅恢复 enum 编译, 不恢复 v14_adapter 映射 (需要时手动加)
```

---

## 7. AC (验收) — 6 项

- **AC80**: enum 删除完成 (cargo build --lib 后变体数 = 42, v17.6 后 48 - 6)
- **AC81**: v14_adapter.rs 同步删除, 无 dead-code warning (cargo build 全过)
- **AC82**: notify.rs PushLevel/dedup/label match 同步删除 (cargo build --lib 0 enum error)
- **AC83**: notify.rs 测试用例 (1801/1824) 改用其他活跃 PushKind (cargo test --lib 通过)
- **AC84**: env var `STOCK_ANALYSIS_V177_LEGACY_KINDS=1` 可恢复 6 项 enum (编译过)
- **AC85**: 历史归档 docs/v15.x/dead-pushkinds.md 包含全部 17 项已删除 PushKind (v17.4 + v17.5 + v17.6 + v17.7)

---

## 8. 决策表 (5 决策)

| # | 决策 | 理由 | 锁定 |
|---|------|------|------|
| 1 | 6 项全部直接删除 (用户选 A) | 全 0 caller, 不值当为 6 个未接线 PushKind 接数据源 | ✅ |
| 2 | v14_adapter.rs 同步删 mapping | 不删会留 dead-code warning, 也表达"彻底清理" | ✅ |
| 3 | 测试用例 (1801/1824) 改用其他活跃 PushKind | Announcement 删后必须改, 否则 cargo test 失败 | ✅ |
| 4 | 历史归档到 docs/v15.x/dead-pushkinds.md (沿用 v17.5/v17.6 模式) | 单一事实源, 不让历史 PushKind 散落各处 | ✅ |
| 5 | env var `STOCK_ANALYSIS_V177_LEGACY_KINDS=1` 回滚 | v15.x 4 铁律 — 所有 BREAKING 变更可回滚 | ✅ |

---

## 9. Commit 列表 (v17.7 = 3 commits, 极轻量)

| # | 主题 | 行数 | AC |
|---|------|------|-----|
| 1 | PushKind enum -6 + notify.rs match 清理 (PushLevel/dedup/label/tests) | ~25 删 + ~20 测试修改 | AC80, 82-83 |
| 2 | v14_adapter.rs 同步删 6 项 mapping | ~10 删 + ~10 测试 | AC81 |
| 3 | 历史归档 docs/v15.x/dead-pushkinds.md + env var 回滚 path | ~12 增 + ~5 测试 | AC84-85 |

总 ~50 行净改动, 3 commits, 1 个工作日工作量。

---

## 10. 风险与回滚

| 风险 | 触发条件 | 回滚 |
|------|---------|------|
| 6 项删除影响未来接数据源 | 用户想接 Wind/Choice 推送研报 | `STOCK_ANALYSIS_V177_LEGACY_KINDS=1` + 手动恢复 v14_adapter (本期不提供完整恢复) |
| v14_adapter 删 mapping 破坏其他引用 | v14 协议路径其他模块用 | 检查 v14_adapter 引用方 (grep `use.*v14_adapter`),若无外部用户则安全 |
| 测试改 PushKind 引入新 bug | 改用 PushKind 后语义不同 | 单测覆盖断言, AC83 验证 |
| dead-pushkinds.md 维护负担 | 后续误恢复 enum, 文档不一致 | env var + 文档双重 source |

---

## 11. 衔接 v17.x 系列

| v17.x | enum 变体 | 0-caller | v17.x 链路 |
|-------|----------|---------|----------|
| v17.4 后 | 58 | 4 | 22 |
| v17.5 后 | 51 | 11 | 32 |
| v17.6 后 | 48 | 14 | 36 |
| **v17.7 后** | **42** | **20** | **36** (无新增, 纯减) |

**v17.7 = 0 新增, 6 减**, 实为历史债务清理。

---

## 12. 待你审批

- [ ] 5 决策确认 (§8 列表)
- [ ] 6 项全删 + v14_adapter 同步策略 (§2.1-2.2)
- [ ] 测试更新方案 (§2.3)
- [ ] env var 回滚路径 (§5.3)
- [ ] 历史归档 docs/v15.x/dead-pushkinds.md (§2.4)
- [ ] AC80-85 验收项
- [ ] Commit 列表 3 个 (极轻量)

批准后 → 写代码 (3 commits × ~25 行, 1 个工作日)

---

## 13. v17.7 实施后 PushKind 总览

| 状态 | v17.6 后 | v17.7 后 | 变化 |
|------|---------|---------|------|
| ✅ L1 EventBus | 36 (75%) | 36 (不变) | 0 |
| ⏸️ push_governor 旧路径 | 4 | 4 (不变) | 0 |
| ❌ 0-caller 废弃 | 14 | 14 + 6 = **20** | +6 |
| **enum 实际变体数** | **48** | **42** | **-6** |

**覆盖率**: v17.7 后 36/42 = **86%** PushKind 走完整 v17.x 链路。
(原 v17.4 §13.10 承诺: v17.7 → 90%, 因为持续发现 0-caller 替代活跃, 实际有效覆盖率略低)

---

## 14. 后续 v17.8 子 spec 启动顺序 (预计最后一批)

按 v17.4 §13.7 表 (减 v17.4-17.7 已处理) — 剩余 8 项交易类:

| v17.x | 处理 PushKind | 主题 |
|-------|--------------|------|
| **v17.8** | EtfClosingCallAuction, BlockTrade*, PostFixedPrice*, StPriceLimitChanged (8 项) | 交易类 |

预计 v17.8 实施后 100% 覆盖 (具体看 grep 实证继续拆分)。

---

**v17.7 spec 364 行完成** — 本批是 v17.x 系列中**工作量最小**的一批, 处理 v14 历史债务。
