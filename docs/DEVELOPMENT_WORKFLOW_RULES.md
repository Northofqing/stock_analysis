# 开发工作流补充规则

## 并行优先

当用户提出包含两个或以上相互独立的开发、排查或验证任务时，默认优先拆分为并行工作流：

1. 先识别任务边界、共享文件和依赖关系；
2. 无冲突的调查、测试、文档和实现分别并行执行；
3. 共享接口、数据库迁移和调度入口必须指定唯一 owner，避免并行写入冲突；
4. 主流程负责整合、冲突解决、完整回归和最终提交；
5. 并行任务必须回传文件证据、验证命令和未完成项，不能只回传结论。

该规则不改变 `AGENTS.md` 的数据安全、资金安全和合规门禁；并行只是任务组织方式，所有分支仍需通过同一套 Gate A-D 验证。
