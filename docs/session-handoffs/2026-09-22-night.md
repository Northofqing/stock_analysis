# 会话交接 — 2026-09-22 夜 (20:11 → 24:00)

> 承接同日 `2026-09-22.md`（20:04 写）。本段：**2 次部署、1 次上游回函、1 个功能实现待部署**。
> master 最新 `79932552`；工作树有**未提交**的 NewsAI counted 准入实现（见 §5）。
> 生产 monitor **PID 34458**，激活 `281ef93a`（21:07:38 重启）。

## 1. 生产运行状态

- monitor **PID 34458**，21:07:38 启动，nohup 直跑（launchd 仍损坏）
- 激活文件 `config/selection/selection_activation.v1.json` = `281ef93a…`（已提交 `f087a57c`）
- 日志 `logs/monitor-launchd-20260916.stderr.log`（**无日期前缀** → 必须锚字节偏移或 `ps lstart`）
- 通道 `SEND_TYPE=feishu`

## 2. 本段两次部署

| 时刻 | PID | 激活 | 内容 |
| --- | --- | --- | --- |
| 20:21:48 | 11011 | `2fdc3e83` | **补做 20:12 未执行的重启** —— Degraded 放行买入生效 |
| 21:07:38 | **34458** | `281ef93a` | **R-12 TechnicalBars 发布**（commit `97038d6d`） |

**为什么会有"补做"**：交接文档写了「20:12:30 重启」，binary 已编译（19:57:16）、activation 已写盘，**但重启从未发生** —— 挂的 cron 是 session-only，随上个会话消失。差一点带着旧树进下一个交易日。见 §7 陷阱 1。

## 3. R-12 发布 —— 结果与预期不同（重要）

commit `97038d6d`：`R12_TECHNICAL_BARS_PUBLISHED` 从编译期 `const false` 改为 env 驱动默认 true，
逃生口 `R12_TECHNICAL_BARS_UNPUBLISH=1`（精确 "1" 触发）。单 gate、生产调用点仅复盘批 follow-up 段。

**部署成功但零产出**：gate 确认放行（`BR-239 disabled` 计数 **0**），但每次执行都失败于上游：

```
R-12 15min bars unavailable for 300274 (阳光电源):
  gRPC TechnicalBars 查询失败: 服务端内部错误  reason_code=no_verified_batch
```

**关键：担心的"6 张卡爆发"没有发生，但只是被推迟了。** 机制（详见记忆
`review-capability-first-publish-burst`）：

- `BACKFILL_SCAN_TRADING_DAYS = 5`（main.rs:5992），R-12 在 `BACKFILL_REVIEW_TASKS` 内
- **BR-200 幂等预检在 capability gate 之后** → gate 关闭期间从不落 claim → 5 个历史日全是欠账
- 失败**不落 claim**（"attempt 失败, 不落 claim, 保留欠账下次扫描再试"）→ **每约 60s 重试**
- 复盘类豁免日预算（BR-237）→ 预算拦不住

⇒ **上游 TechnicalBars 恢复的那一刻，5 个历史交易日 + 今日会一起成功并爆发推卡。**用户已明确接受。

## 4. 上游交接（grpc_handoffs/）

**我方回函**：`2026-09-22-review-upstream-failures-downstream-reply.md`（commit `79932552`）

**三处纠正上游假设**：

1. **R-08 ≠ `MarketAnnouncements`**。上游据 `docs/data-sources-inventory.md` 单源推断。
   实测 R-08 是**四源组合**：`R-08-announcements`(Cninfo) + `R-08-cffex-delivery`(**Cffex**) +
   `R-08-global-indices`(Sina) + `R-08-global-fx`(Sina)。19:00 整任务 failed 的组件是
   **`FuturesDelivery`**，归类 `invalid_evidence`。⇒ 上游 §1 的二分法（`internal` ⟹ 只可能
   来自公告链路）在多源 R-08 上不成立。
2. **R-09 故障不是 VM 特有**。我在 **Mac 宿主**复现同形失败，且发现**宿主自身也在 fake-IP
   代理下**（`push2.eastmoney.com → 198.18.49.0`；`www.eastmoney.com` 同为 fake-IP 却 200 →
   同代理按域名分流）。用真实 IP（`61.129.129.196`）`--resolve` 直连仍瞬时拒绝 →
   **我方无法做 (a)代理规则/(b)出口 IP 反爬 的判别**，需独立出口。
   附报一处不一致：上游记录 VM 内 `ulist.np/get` 返回 200，我在宿主测**失败**。
3. **我方 HistoricalBars 走「带显式日期范围」形状**（`historical_bars.rs:50` `window_start`），
   **不在**上游 §6 确定性矩阵覆盖范围内 → 请其按正确形状复测。

**接受上游两处纠正（我方旧结论作废）**：`部分码 Baidu 成功` 撤回（Baidu 一律 scope decline，
无逐码状态）；`逐码 flaky` 撤回（各路由失败均为确定性合同/准入事实；Tdx 解码缺陷已被上游认领）。

**文档修正**：`docs/data-sources-inventory.md` 补多源组合告警表 + `EconomicCalendar` 退役标注。

## 5. NewsAI counted 准入 —— 已实现、已复审、已修缺陷，**未提交、未部署**

目标：NewsAI 分析卡此前**绕过 counted 协调器**直推（无决策行、不受预算/冷却约束），
把它接进 counted 准入层。

### 5.1 设计定案（与旧计划文档不同，务必以此为准）

- **新增 `PushKind::NewsAiAnalysis`**（`DurablePushKind` 同名变体）。
  ⚠️ **不是**复用 `NewsToIdea` + 新 sub_kind —— `requested_sub_kind` 覆盖通道**只服务
  `DailyReport`**，`K::NewsToIdea` 硬编码 `DeliverySubKind::None`；复用就得把语义上专属
  日报的 `DailyReportSubKind` 塞进 NewsAI（伪复用）。
- 策略行 `(NewsAiAnalysis, PerTicket, Some(1200), Rolling)`，**`counts_against_daily_budget = false`**
  （用户决策：AI 卡 55 张/日会吃穿 30 槽）。`POLICY_VERSION` **6 → 7**。
- 双状态机并存：**counted 决策行 = 投递准入权威；BR-172 行 = NewsAI 专属审计链**。
- 一张卡产生**两条投递审计行**（counted + NewsAI 专属，后者喂 BR-172 `record`）。
  复审确认无重复计数、无幂等问题。

### 5.2 改动清单（未提交）

`src/durable_delivery/{model,schema,tests}.rs`、`src/bin/monitor/{notify,news_ai_shadow,
durable_delivery_runtime,presentation_registry,push_templates,br196_test_delivery,main,v14_adapter}.rs`、
`src/monitor/news_ai.rs` —— 共 12 文件 +675/−78（含本段下述修复）。

### 5.3 对抗性复审结果

**无 P0**。最担心的两类致命缺陷**均已排除**：① 准入拒绝不留悬挂 `Reserved`/`SinkStarted`；
② 不存在"先物理外发再被拒"（counted `prepare` 拒绝路径根本不调 sink）。

通过核对：预算豁免、POLICY_VERSION 自洽、无冷却桶/身份碰撞、binding 稳定性、
双审计无危害、kind 登记完整性。

**已修的 3 条**：

| 项 | 问题 | 修法 |
| --- | --- | --- |
| **F1** | 准入拒绝被记成 `failed` + WARN（`run_same_tick_batches` 无 `Denied` 臂）→ 真实故障与"按设计丢弃"无法区分，55 张/日下常态化假阳性 | 单开 `admission_denied=` 计数、info 级 |
| **F2** | 两条"旗舰"测试**没覆盖宣称的路径**；唯一碰映射的源码 grep 测试漏断言 `AdmissionDenied` → 若误映射为 `PostSinkFailure`，BR-172 会落 `PostSinkRecovery` 并**永久 Dedup 卡**，全套测试不报 | 补 `AdmissionDenied`/`PhysicalPushOutcome::Denied` 断言 |
| **F3** | 新 kind 漏登记 `requires_banner()` → counted 门治理上下文从 `CountedCombinedAccount` 静默降到 `CountedSourceOnly` | 加入列表（对齐 D-01）；**故意不加** `is_live_trading_kind`（D-01 也不在）；补一致性断言 |

**未修（新会话接）**：

- **F4**：`PreSinkError` / preflight `Error` 映到 `SinkError` → `post_sink_recovery` 要求
  状态是 `SinkStarted`，而此刻是 `Reserved` → 持久化失败只把错误拼进 reason，误报
  `PostSinkRecovery`。**低危 + 可自愈（下轮 reserve 命中 `Reserved` 复用重试）+ 当前不可达**
  （dry-run/V6 已由 preflight 拦下；`acquire_token` 字面量同二进制）。
  修它=改投递状态机终态语义，**当时上下文耗尽，故意停手**。
- **F5**：被拒卡每轮重试写 3 条 BR-172 行（`rollback`→`RolledBack`→可重预留，每轮新
  `reservation_id`）。热点票 1200s 冷却 ≈ 10 个 tick → 数十行账本。**无重复外发**。
  属设计题（要动重预留策略）。
- **F2 深层**：仍缺真正穿过 `send_preflighted_news_ai_analysis_v3` 的端到端测试。
  需要 counted 协调器测试夹具（测试命名空间 + 预置 cooldown head 造 Denied）。

### 5.4 ⚠️ 一个影响部署判断的结论

**dry-run 根本不走 counted 派发**（counted 家族以
`capability_unavailable=…_counted_binding_unavailable` 跳过）⇒ **没有任何实测路径真正验证过
NewsAI 的 counted 准入**。`--test --push-dry-run` EXIT 0 **不能**作为它的验收证据。
首次真实验证只能等部署后盘中。

### 5.5 w12 golden 重锁（本段一并处理）

`w12_legacy_envelope_keeps_exact_identity_and_canonical_bytes` 在 **HEAD 上就失败** —— 其
golden 自 `cbb3e6b7` 起从未随 `POLICY_VERSION` 1→2→3→5→6 重锁。

**查实是设计使然非缺陷**：`DecisionIdentityMaterial`（model.rs:1047）显式含 `policy_version`；
schema.rs 注释写明 "the POLICY_VERSION bump yields fresh decision identity"。

**且 canonical 字节 golden 也随版本变**（`canonical_bytes()` 同样含 policy_version）——
本测试**没有任何版本无关部分**。已重锁两个 golden + 加版本标注，并在 `POLICY_VERSION`
定义处写明「**bump 必须同步重锁**」。用户已批准。

## 6. 本段验证汇总（实测）

| 项 | 结果 |
| --- | --- |
| `cargo test --bin monitor push_templates` | 335 passed / 0 failed |
| `cargo test --bin monitor notify::` | 66 passed / 0 failed (1 ignored) |
| `cargo test --bin monitor news_ai_shadow::` | 8 passed / 0 failed |
| `cargo test --bin monitor br196_test_delivery::` | 7 passed / 0 failed |
| `cargo test --lib w12_legacy_envelope…` | ok |
| `cargo run --bin monitor -- --test --push-dry-run` | **EXIT 0**，`failed=0 families=59` |

**预存 flaky（非本段引入）**：`durable_delivery` 家族顺序依赖失败，测试名**逐轮变化**，
单跑各自通过。`data/test` 下现存 **10,410 个遗留 `TEST_CODE_*` 目录**（Aug 1 起）为共享竞争源，
`--test-threads=1` 反而更糟。判断回归**必须比测试名差集，不能只看数量**。

## 7. 本段新发现的陷阱（已写入记忆）

1. **部署"看起来完成"≠ 生效**：binary 编译好 + activation 写盘 ≠ 部署完成。
   cron 是 session-only ⇒ 重启可能静默未发生。新会话接手**必做**：
   `ps -eo pid,lstart` 实测进程 + `CronList`（几乎必空）+ **实跑 `print-activation` 核对
   磁盘树 hash 与 activation 逐字一致**。→ 记忆 `staged-deploy-restart-liveness-check`
2. **BR-183 的 hash 覆盖全部 `src/**`，不是只有 `src/config/`**。
   本段有 agent 判断「未改 src/config 所以不需重发激活」——**实测证伪**：改
   `push_templates.rs` 即把 hash 从 `2fdc3e83` 变为 `281ef93a`。照此判断部署会重演
   16:25 的 `activation_not_effective`（selection-v2 静默关）。
3. **activation 文件写盘约定** = 紧凑 JSON + **单 LF**（`"}\n`）。
   `print-activation` 只打印不写盘，且要求 `effective_from` 在**未来**、重启须晚于它。
4. **复盘能力首发必然爆发**（见 §3）→ 记忆 `review-capability-first-publish-burst`
5. `cargo test --lib` 残渣库（`$TMPDIR/stock-analysis-unit-*.db*`）不自动清理，堆积会整轮挂死。
   本段清了 21 个。
6. **`docs/plans` 与 `docs/audits` 的 gitignore 差异**：`docs/plans` 被 gitignore 但本段
   编辑的文件已被跟踪，故能入库；新文件则会静默不入库。

## 8. 未完成清单（按优先级）

1. **NewsAI counted 准入部署**（§5，代码已就绪未提交）：
   `cargo build --release` → **重发激活**（本段又改了 src，先前 hash 已失效，需重测）
   → **第三次重启** → 验证。**F4/F5/F2-深层**同批处理更省一次部署。
2. **R-12 爆发观察**：上游 TechnicalBars 恢复时会有 5+1 张卡涌出（用户已接受）。
   若要抑制需给复盘任务加失败重试上限（同 T-03 每码 3 次/日 思路）。
3. `EconomicCalendar` 休眠链：`chain_post_close_macro_*` 无生产调用者，但一旦启用会调
   已退役 operation（只返回 `UNIMPLEMENTED`）。已在 inventory 标注，**未加防护**。
4. 竞价 A-02/P-05（上游待修）、评估 #3/#5、30 槽按 kind 隔离（设计债）。
5. 交接文档 §6 小项：11 Uncertain 决策待裁定、公告漏斗 pushed=0 观察。

## 9. 明日自动任务

**无 cron**（本段所有 session-only cron 已随会话消失，CronList 为空）。
R-12 原计划的「明 15:30 部署链」**已由本段提前完成**（21:07 生效）。
19:00 复盘批会自然触发 R-12（若上游恢复）。

## 10. 相关提交

```
79932552 docs: 回应上游 R-08/R-09/HistoricalBars 核实 + 修正两处被推翻的前提
f087a57c chore(activation): BR-183 重发激活 — R-12 发布树 (281ef93a, 21:05 生效)
97038d6d feat(review): R-12 TechnicalBars 发布 — BR-239 gate 放行 + env 逃生口
a7425596 docs: 2026-09-22 会话交接 (上个会话, 20:04)
```
