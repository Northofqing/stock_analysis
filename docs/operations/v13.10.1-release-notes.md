# v13.10.1 Release Notes — 推送数据修正与降噪

> **版本**：v13.10.1 (P0 数据修正 + P1 刷屏治理)
> **触发原因**：2026-07-08 push_log 5 项用户反馈
> **关联 spec**：`docs/architecture/v13-push-templates.md` + v13 模板治理
> **实施日期**：2026-07-08

---

## 1. 触发问题（基于 2026-07-08 push_log）

| # | 用户反馈 | 实际 push_log 表现 |
|---|---|---|
| 1 | 候选筛选台"通过硬门槛 0 只" | `15:05 候选筛选台 · 通过硬门槛 0 只` 空卡片 |
| 2 | 虚拟观察仓次日涨跌数据错 | `15:03 时空科技 1000股 入场¥86.46 -> 收盘¥120.06 \| +38.86%` （跨 13 日） |
| 3 | 优选候选没和候选筛选台合并 | `15:02 优选候选：产业链信号可信度不足` + `09:30 AuctionRepush` 重复通道 |
| 4 | 交易复盘平仓复盘数据错 | `15:00 交易复盘` 6 条全 `合肥城建(002208) -9.2%` 重复 |
| 5 | 今日复盘 T+1 冻结/明日预警数据错 | `15:00 T+1 冻结股明日预警` 7 只全 `止损 0.00` 无意义 |
| 6 | 跌停通知只推送无意义 | `09:56-10:05 先导基电 跌停` 10 分钟 6 条 |
| 7 | 选股推荐只推送无意义 | `09:30 选股推荐` 6 只置信度 30-45 |

---

## 2. 修复清单

### 2.1 P0 数据错（必修）

| Bug | 文件 | 修复点 |
|---|---|---|
| #4 平仓重复 | `src/review/journal.rs:29-58` | `review_closed_trades` 入口按 `(code, dir, date, price, shares)` 去重 DB 重复行 |
| #4 持仓中价格缺失 | `src/review/report.rs:101-119` | 拉不到实时价时显示 `数据不足` 后缀, 不再用 cost_price 顶替 |
| #5 T+1 字段语义 | `src/monitor/checklist.rs:82-138` | `build_close_summary` 拆分 `t1_frozen` (今日冻结明日解禁) 与 `tomorrow_unlocks` (今日解禁明日可卖) 两段; 止损=0 显示 "止损未设" |
| #5 close_summary 调用方 | `src/bin/monitor/main.rs:4859-4890` | 重新按 `is_t1_locked` 分类 7 只持仓 |
| #2 虚拟观察 T+1 收盘 | `src/bin/monitor/main.rs:238-274` | 新增 `fetch_t1_close_map(codes, base_date)` 取 `snapshot.created_at` 后第 1 个交易日 close; 解析 `parse_snapshot_base_date` |
| #1 候选台空 | `src/bin/monitor/main.rs:5369-5372` | `run_candidate_panel_from_review` `entries.is_empty()` 时直接 `return ""`, 不再推"通过硬门槛 0 只"空卡片 |
| #3 合并优选 | `src/bin/monitor/main.rs:3946-3953` | 9:20-9:25 删 `AuctionRepush` 独立推送, 仅拉候选用于虚拟观察 |
| #3 候选台注入 | `src/bin/monitor/main.rs:3332-3351` | `run_review_deep_analysis` 主流程时, `optimal_close_raw = Some(&run_post_close_candidates(5).await)`, 优选并入候选台 |

### 2.2 P1 刷屏治理（应该修）

| Bug | 文件 | 修复点 |
|---|---|---|
| #6 跌停 per-day dedup | `src/monitor/signal_state.rs:33-50, 95-110` | 新增 `once_per_day: HashMap<String, NaiveDate>`, `process()` 入口对 `AlertCategory::LimitDown` 做同日单次去重; `daily_reset` 一并清空 |
| #7 选股推荐阈值 | `src/bin/monitor/main.rs:5094-5100` | 置信度阈值 20 → **50**, Top 3 不变 |

---

## 3. 测试覆盖

新增 5 个单测 + 1 个扩展：

```rust
// src/review/journal.rs (现有 test_review_basic 覆盖)
test_review_basic       ✅
test_review_fifo        ✅
test_no_review_for_holding ✅

// src/monitor/checklist.rs (新增 3)
test_close_summary_with_t1_frozen       ✅  // 验证 t1_frozen 段标题
test_close_summary_with_tomorrow_unlocks ✅  // 验证 tomorrow_unlocks 段标题
test_close_summary_hides_zero_stop       ✅  // 止损=0 显示 "止损未设"

// src/monitor/signal_state.rs (新增 1)
test_limit_down_once_per_day             ✅  // daily_reset 后能再次触发

// 6 个新单测 + 3 现有 单测全部 PASS
```

全量测试：192/201 PASS（9 个失败为 `数据库未初始化` 预存在测试，与本 PR 无关，已在 master 验证）

---

## 4. 不变项

- v13 模板 §14.0-14.5 PushKind 治理 100% 不变
- 6 dispatcher 真实数据流通路不变
- `Cargo.toml` version 不变（沙箱里 Cargo 锁 0.1.1, v13.x 走文档版本号；v13.10.1 仅在 release notes / 代码注释中标注）

---

## 5. 部署

```bash
git pull origin master
cargo build --release --bin monitor
./target/release/monitor --push-dry-run
# 期望: dispatcher_log.jsonl 不再出现 "候选筛选台 · 通过硬门槛 0 只"
#       跌停仅当日首推
#       选股推荐仅置信度 ≥50
```

---

## 6. 关联

- 设计 spec: `docs/architecture/v13-push-templates.md`
- v13 release: `docs/operations/v13-release-notes.md`
- v13 部署: `docs/operations/v13-push-templates-deployment.md`
- 触发 push_log: `data/push_log/2026-07-08/`

**v13.10.1 = 推送数据正确性 + 噪声治理。**
