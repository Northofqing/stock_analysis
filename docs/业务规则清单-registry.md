# 业务规则清单

> **MUST** (AGENTS.md §2.10) 任何"去重 / 互斥 / 过滤 / 排序 / 限额"的业务规则必须登记在本文件。每条含编号、规则描述、代码位置、测试位置、最后审核日期。
> **MUST** 任何新代码涉及上述类别, 必须先登记再写实现。

| 编号 | 类别 | 规则 | 代码位置 | 测试位置 | 末审 |
|------|------|------|---------|---------|------|
| BR-001 | 去重 | 同一只票近 3 个日历日最多推送 1 次 (注: 实现用日历日而非交易日, 简化 YAGNI — 周末不推送, 跳过周期可接受) | `src/opportunity/discover.rs::discover` | `tests/e2e_dedup.rs` | 2026-06-29 |
| BR-002 | 互斥 | 一条快讯最多命中 1 条产业链。例外: AI 推理明确给出 ≥2 条**独立**产业链 (chain 名之间无包含关系、关键词不重叠) 可保留。 | `src/opportunity/chain_mapper.rs::map_news_to_chains` | `tests/chain_exclusive.rs` | 2026-06-29 |
| BR-003 | 过滤 | 宏观新闻 (美联储/美股/汇率/大宗) 入 macro 通道, 不入 chain_mapper | `src/search_service/service.rs::fetch_flash_titles` | `tests/flash_filter.rs` | 2026-06-29 |
| BR-004 | 排序 | 推送 TopN 按 final_score 降序, 同分按发布时间升序 | `src/opportunity/mod.rs::run_post_close_candidates` | (已存在 `tests/ranking.rs` 验证) | 2026-06-29 |
| BR-005 | 限额 | 每个本地交易日成功投递的 `CandidateBoard` 机会推送 ≤ 5；计数只读取 L7 `push_analytics` 中 `pushed=1` 的真实投递记录，达到上限后 L5 返回 `daily_limit`，扫描结果仍保留在候选扫描/事件总线链路而不对外发送。计数必须按持久化 RFC3339 时间戳携带的本地民用日期分日，禁止让 SQLite `date()` 隐式转 UTC 后把上海 00:00–07:59 错算到前一天。计数存储不可读时必须拒绝该类推送，禁止按 0 放行。 | `src/bin/monitor/v14_adapter.rs` + `src/push_l7/sqlite_store.rs` | `src/bin/monitor/v14_adapter.rs` + `src/push_l7/sqlite_store.rs` + `tests/ranking.rs` | 2026-07-19 |
| BR-006 | 过滤 | 基于真实胜率关停 0% 主题。`chain.toml` 中 `enabled = false` 的规则在 `chain_mapper::map_chain_rules` 阶段被过滤, 后续推送不再命中。当前关停清单 (14 天回测): **6 个 enabled=false** = 5 真 0% (AI硬件-液冷 6/0, 半导体-先进封装 12/0, 消费电子 6/0, 新能源-电池 10/0, 稀土永磁 15/0) + 1 极低 (稀有金属 89/1 = 1.1%, 留观察). winrate_simulator 默认 blacklist 自动从 config 读 (修复 I-3 2026-06-29 codex review: 之前 spec 写 "5 关停" 是稀有金属 1.1% 仍 >0% 不算 "真 0%", 但仍差到关停). **BR-006 决策修订 (v9.2 PR-3 数据回灌后)**: 早期一轮关停的 6 个主题 (半导体-制造代工 / 创新药-CXO / AI硬件-MLCC / Rubin / AI算力 / AI硬件-CPO) 在 PR-2 修复 R-3 数据断层后, 真实胜率显现非 0%, 已重新开启并按真实胜率加权 (半导体-制造代工 64.6% → priority 88, 创新药-CXO 36.8% → priority 85)。教训: 早期 0% 是数据缺失假象, 不是真信号, BR-006 触发必须以 "真 verify 完成的样本" 为前提。再启条件: 跑 `winrate_simulator` 验证 ≥10% 胜率。 | `src/opportunity/chain_mapper.rs::map_chain_rules` + `config/chain.toml` | `src/opportunity/chain_mapper.rs::test_br006_disabled_chains_excluded` + `src/bin/winrate_simulator.rs` | 2026-06-29 |
| BR-007 | 限额 | 季度 winrate review 流程。`tools/one_shot/winrate_review.sh` 每季度自动跑 (建议 crontab: `0 9 1 */3 *`), 跑 backfill + simulator + 输出 markdown 报告到 `reports/winrate_review_YYYY-MM-DD.md`。报告含: 关键指标 (胜率/推送数/pending) + simulator 决策建议 + 历史对比表 + 下一步 action items。AGENTS §2.4 数据驱动决策循环。 | `tools/one_shot/winrate_review.sh` | (脚本本身是测试 — 跑一次无副作用) | 2026-06-29 |
| BR-008 | 排序 | 数据驱动加权主题 (priority ≥ 90)。基于真实胜率, 当前加权: AI硬件-PCB 95 (66.7% / 120 推送), 半导体-设备 96 (73.2% / 41 推送), 新能源-固态电池 96 (100% / 7 推送), 新能源-钠离子电池 92 (33.3% / 21 推送), 新能源-锂电池 80 (45.5% / 11 推送)。priority 越大越优先 (BR-002 互斥下高 priority 先匹配)。新增加权: 跑 `winrate_simulator` 输出 ≥30% 主题 → 评估样本量 (≥15 推荐加权, 5-15 保守加权) → 改 `chain.toml` priority + 加 AGENTS §2.9 边界证明注释 + commit. | `config/chain.toml` (priority 字段) | `src/bin/winrate_simulator.rs` 输出 | 2026-06-29 |

| BR-009 | 限额 | monitor 任何工作流必须有显式超时。`run_review_only` 默认 5min timeout (env `MONITOR_REVIEW_TIMEOUT_SECS` 可覆盖, 0/负数/非法值 fallback 到 300s), 超时未完成时 `log::error!` + `log::logger().flush()` + `std::process::exit(2)`, **不** push_wechat 推送噪声给用户。修复 P0-G (2026-06-30 codex review): 沙箱 / 数据源全失联时, 进程可能在 reqwest 内部回调里死锁, 无超时则无 exit code 反馈, 掩盖所有下游问题。AGENTS §2.1 数据源失败显式报错。| `src/bin/monitor/main.rs::run_review_only` (line 653) | `tests/review_timeout_tests.rs` (6 tests) | 2026-06-30 |

| BR-010 | 限额 | 多 Agent 研判必须 freshness 校验。`run_multi_agent_analysis` 与 `run_multi_agent_analysis_with_seed` 在拿到 K 线后必须 `check_kline_freshness(code, &kline)`，基于 `validate_daily_freshness(last.date, now, &config, &stats)`（config 默认 `dq_daily_stale_sec=86400`，1 交易日阈值）。数据过期时 bail + `[DQ_FRESHNESS] data_type=Daily` 日志，**不**继续 LLM 研判，**不** push_wechat。修复 P0-E (2026-06-30 codex review): 之前 `service::service().get_kline` 缓存层无 freshness gate，35+ 持仓 freshness reject 后多 Agent 研判照样跑、照样推送，违反 AGENTS §2.1 / §2.4。| `src/deep_analyzer.rs::check_kline_freshness` (新增 line 36) + `run_multi_agent_analysis:191` + `run_multi_agent_analysis_with_seed:317` | `src/deep_analyzer.rs::tests_br006` (4 tests) | 2026-06-30 |

| BR-011 | 限额 | 多 Agent 6 工具失败必须显式标注，禁止静默填空字符串。`ToolResult::classify` 检测两类失败：(1) `Result::Err` → MISSING；(2) `Result::Ok` 但内容含 `"error"`/`"Error"`/`空字符串` → MISSING（**假成功**检测）。`render_data_inventory` 渲染 `[name] OK` / `[name] MISSING: <reason>` 注入 LLM `basics` slice 头部，`DATA_INVENTORY_RULE` 强制约束 "MISSING 字段必须写 [数据缺失]，禁止编造" + "综合分 OK<2 时标 N/A"。修复 P1-F (2026-06-30 codex review): 之前 `unwrap_or_warn` 把 `Ok({"error": ...})` 当成功传给 LLM，6 tool 失败时 LLM 编造 1,500 字研判，违反 AGENTS §2.1 / §2.8。| `src/deep_analyzer.rs::ToolResult::classify` + `render_data_inventory` (line 36) + `run_multi_agent_analysis:300` | `src/deep_analyzer.rs::tests_br011` (8 tests) | 2026-06-30 |

| BR-012 | 限额 | 北向资金缺失必须显式标注，禁止隐式 0.0。`MarketOverview.north_flow: Option<f64>`（不再 `f64` 默认 0.0）。`async_overview::fetch_north_flow_blocking` 内部 `unwrap_or(0.0)` 是假成功点，已在 `get_market_overview_blocking` 拦截：`Some(0.0) → None` + warn 日志。`format_market_report` / `review.rs` 模板字符串 `| 北向资金 | {} |`（不再 `{:+.2}亿`），`None` 时渲染 `[数据缺失]`，`Some(v)` 时渲染 `{:+.2}亿`。修复 P1-3 (2026-06-30 codex review): 之前 "北向资金 +0.00亿" 噪声永远出现，违反 AGENTS §2.1 / §2.2。| `src/market_data.rs::MarketOverview.north_flow` (line 111) + `src/market_analyzer/async_overview.rs:302` (format) + `src/market_analyzer/review.rs:71,188` (prompt 模板) | `tests/north_flow_option_tests.rs` (5 tests) | 2026-06-30 |
| BR-013 | 排序 | 持仓多 Agent 研判必须按操作建议聚合推送，禁止 7 只持仓 7 条独立推送。`run_review_deep_analysis` 收集所有结果后调 `build_holding_summary` 聚合：`extract_advice_and_score` 从 LLM markdown 提取 `## 【操作建议】` 段第一行 + 综合分数字；按"强烈卖出 > 卖出 > 减持 > 观望 > 买入 > 强烈买入"优先级排序；同组聚合成 `【减持】(4 只)` 块。模板按综合分裁剪：`< 30` 或 `> 70` 简短 (200 字)，`30-70` 详细 (800 字)。每只票 md 仍落盘到 `save_deep_report` 供事后查。修复 P1-1 (2026-06-30 codex review): 之前 7 条 1,500 字推送同质化严重，违反 AGENTS §2.10 业务规则文档化精神。| `src/bin/monitor/main.rs::build_holding_summary` + `extract_advice_and_score` + `first_meaningful_line` (line 942+) | `tests/holding_summary_tests.rs` (8 tests) | 2026-06-30 |

| BR-014 | 限额 | event_risk_score 数据不足封顶必须严格小于 push threshold。当前口径由规则 BR-096 收口：推送门 `T` 只读 `config/strategy.toml::opportunity_push_threshold`，数据不足或 winrate 缺失/非正时 `event_risk_score <= T-1 < T`；不再存在默认开启的 70 分灰度例外。AGENTS §2.9 的 `threshold > clamp_max` 针对总分值域上限 100，并由机器合同 fail closed 校验。| `src/opportunity/score.rs` + `src/opportunity/mod.rs` + `config/strategy.toml` + `config/design_contracts.toml` | `src/opportunity/score.rs::tests_r2` + `tests/test_design_contradiction.rs` | 2026-07-17 |

| BR-015 | 限额 | 产业链集中度检查当前禁用, hardcoded 0, 必须在 stock_position 表加 chain_name 列后启用。`position_tracker.rs:314-340` 中 `PositionSizer::max_position(_, _, 0, 0, _)` 调用处两个 0 均为 placeholder; 当前监控无法拒绝同产业链的第 N+1 只建仓。修复 P2-1 (2026-06-30 codex review): 之前注释引用 TODO B6 没有 BR 编号, 违反 AGENTS §2.10 业务规则文档化要求。完整启用需要 (1) schema migration 加 chain_name 列 (2) open_position 时存 chain_name (3) DB 查同 chain 持仓 / T+1 冻结数 (4) chain_mapper 关键词表算 chain。| `src/pipeline/position_tracker.rs:314-340` + `src/risk/cash_guard.rs` (下游) | 无单测 (待 v9.4+ 接 broker API 时统一加 chain_name 列后补测) | 2026-06-30 |
| BR-016 | 排序/分类 | v10 P0 虚拟仓理由固定枚举 `VirtualReason { Breakout, VolumeSurge, MainNetInflow, SectorLeader, NewsCatalyst, AuctionAnomaly }`。每条虚拟仓/预测记录带 1 主理由 + 可选副理由, 写入 `prediction_tracker`; 结算按主理由分组统计胜率。**多理由命中时按固定优先级取主理由 (四角挑战 BC-6)**: `NewsCatalyst > AuctionAnomaly > MainNetInflow > SectorLeader > Breakout > VolumeSurge`, 其余记副理由。**归属 (BC-6)**: 枚举作 Opportunity context 值对象, Portfolio 存 reason_name snapshot 字符串。**禁止自由文本**避免统计口径漂移; 新增理由走枚举变更 migration + 本文件更新。设计见 `docs/architecture/v10-intraday-monitor-and-review-2026-07-01.md` §10.3。**先登记后实现 (AGENTS §2.10)**: P0 编码前登记, 代码已落地。 | `src/opportunity/virtual_reason.rs` + `migrations/v10-p0-1-virtual-reason/up.sql` | `src/opportunity/virtual_reason.rs` | 2026-07-01 |
| BR-017 | 排序 | v10 胜率统一口径 (single source of truth = `prediction_tracker`)。一条 prediction 同时回填 T+1/T+3/T+5 三窗口 actual_change + hit; 胜率按 reason 分组 + 盈亏比加权 (`pnl_total/capital_total`, 复用 `main.rs::build_virtual_next_day_review_text`)。`winrate.rs::calc_winrate_score` 从独立 `BacktestSample` 收敛为读 `prediction_tracker`, 消除三套分裂口径。**hit 定义含 A 股制度特例 (四角挑战 BC-3)**: 停牌→NULL(不算胜率)、涨停→按买入价能否买进判、跌停→false、正常→(actual_close−buy)/buy>0.5%; `t1_special_case` 列记录特例类型。口径 (方向+阈值+窗口+特例) 写入表注释。设计见 v10 §5 / §5.2。**先登记后实现 (AGENTS §2.10)**。 | `src/opportunity/winrate.rs::load_reason_winrate` + `src/database/mod.rs` | `tests/winrate_tests.rs` + `tests/opportunity_e2e_tests.rs` | 2026-07-01 |
| BR-019 | 过滤/标签 | v10 异动即时归因 (G5a)。`detector` 判异动时, 通过**领域事件 `AttributionRequested` (四角挑战 BC-4 解耦)** 调 `chain_mapper` **规则关键词路径** + `news_ai` 快归因 (P95≤2s, **不调 LLM、不用 T+1 龙虎榜**; AI 深链归 G5b), 结论回写 `AlertDetail.ai_decision` + 落 `alert_log.jsonl`。判定"查到催化" = ≥1 源强证据 (news_ai importance≥3 **或** chain_mapper 命中非 generic 链 **或** multi_agent composite≥60); 否则标「⚠️ 异动查无催化」风险标签, 不静默留白 (AGENTS §2.2)。**P1 交付附 chain_mapper 精度基准真催化占比≥70% (BC-2)**。走 monitor 通道, 不占 BR-005 预算 / 不受 BR-001 去重。设计见 v10 §4.5。**先登记后实现 (AGENTS §2.10)**: 代码已落地。 | `src/monitor/attribution.rs` + `src/monitor/alert_log.rs` + `src/bin/monitor/main.rs` | `src/monitor/attribution.rs` | 2026-07-01 |
| BR-020 | 排序 | (四角挑战 BC-1) v10 胜率必对标 benchmark, 避免 selection bias。推送胜率含大盘 beta, 真 alpha = `push_winrate − 同日同窗市场涨幅`。`prediction_tracker` 加 `market_up_rate_t1/t3/t5` 基准列 (当日全市场上涨家数占比或指数涨幅), 结算时回填。按 reason 分组统计时, **样本<20 标「样本不足」、<10 不作决策依据** (避免 3/3=100% 假胜率)。设计见 v10 §5 / §11.1。**P0.3 落地 (2026-07-01)**: `src/opportunity/real_alpha.rs::compute_real_alpha` + `tools/one_shot/backfill_predictions.sh` (Q2=B 全市场上涨家数, 2158 条已 verify, market_up_rate_t1 写入 23 条)。Q4=C 动态阈值 `max(20, total_pushes*0.05)` 已在 `src/opportunity/virtual_reason.rs::compute_sample_threshold` 实现。 | `src/opportunity/real_alpha.rs` + `src/opportunity/virtual_reason.rs` + `tools/one_shot/backfill_predictions.sh` | `src/opportunity/real_alpha.rs::tests` (15 PASS) + `src/opportunity/virtual_reason.rs::tests` (15 PASS) | 2026-07-01 |
| BR-021 | 限额 | v12 PR1 账户模式三态判定 (AccountState ∈ {Normal, ReduceOnly, Frozen})。**触发降级 (Normal→ReduceOnly)**: 当日累计盈亏 ≤ `-daily_loss_pct` (默认 -1.5%) **或** 连续止损笔数 ≥ `consecutive_stop_loss_n` (默认 3 笔)。**触发熔断 (ReduceOnly→Frozen)**: 当日累计盈亏 ≤ `-circuit_breaker_pct` (默认 -2.0%) **或** 总仓位 > `position_overload_cheng` (默认 8 成). **解除条件**: 下一交易日盘前重置为 Normal. **无数据时保守取 ReduceOnly** (不假设 Normal). 数据源: `position_tracker.rs` 复用费率 const (`:37-42`). 设计见 `docs/architecture/v12-trading-assistant-implementation-2026-07-04.md` §2.3. **先登记后实现 (AGENTS §2.10)**: 代码 PR1-1.3 落地. | `src/risk/account_mode.rs` | `src/risk/account_mode.rs` | 2026-07-05 |
| BR-022 | 互斥 | v12 PR1 动作门 (ActionGate) 权限矩阵 6 动作 × 3 模式 = 18 格. `ActionKind ∈ {OpenNew, Add, Reduce, Clear, T0Positive, T0Reverse}`. **Frozen**: 6 动作全 Deny (blocked_reason: "账户熔断, 禁止任何新动作"). **ReduceOnly**: OpenNew=Add=Deny (禁建仓); Reduce=Clear=Allow; T0Positive=Deny ("只允许减仓, 不允许做T加仓"); T0Reverse=Allow ("反T接回底仓允许"). **Normal**: 6 动作全 Allow. **不动 `veto_chain`**: ActionGate 是新建的窄接缝, 不替换现有 veto_chain.rs:83-94 的 VetoRule 框架 (v12.2 §2.4 决策). 设计见 v12 §2.3 + `v12-trading-assistant-implementation-2026-07-04.md`. **先登记后实现**: 代码 PR1-1.2 落地. | `src/risk/action_gate.rs` | `src/risk/action_gate.rs` | 2026-07-05 |
| BR-023 | 隔离 | v12 PR3 虚拟盘腿与实盘腿硬隔离. **虚拟腿**只写 `paper_trades` 表; **真实减仓**走 `position_adjustments`. **禁止**虚拟腿写 `stock_position` (v12.2 §2.4 + 实测 2026-07-05: 14 处调用点 / 9 文件, 改语义风险高). 设计见 `v12-trading-assistant-implementation-2026-07-04.md` §10. **先登记后实现**: 代码 PR3-3.5 `src/trading/paper_trade.rs` 落地. | `src/trading/paper_trade.rs` | `src/trading/paper_trade.rs` | 2026-07-05 |
| BR-024 | 限额 | v12 PR3 position_adjustments 写入入口. **写入场景**: (1) 人工确认减仓 (source='manual_confirm'); (2) 同日加仓记录 (source='import'). **env_guard**: 测试环境 (STOCK_ENV_MODE=test) 拦截写入 (对齐 `src/database/positions.rs:21/:111` 双向隔离). **delta 语义**: 负 delta 即时生效 (减仓 → available_shares 立即下降); 正 delta T+1 生效 (同日加仓 → 当日 available 不增). 设计见 v12 §10.2. **先登记后实现**: 代码 PR3-3.3 落地. | `src/database/position_shares.rs` | `src/database/position_shares.rs` | 2026-07-05 |
| BR-025 | 限额 | v12 §14.3 PushKind 清单与治理规则. 新 PushKind 13 个: AccountMode/DataMode/HoldingPlan/T0Advice/CandidateTriggered/ForbiddenOps/PaperTrade/CloseCall/ReviewMarket/ReviewLhb/ReviewSignal/ReviewFailure/TomorrowWatch/EventCalendar 走 `PushKind::level()` 表决定 🚨/⚡/ℹ️ 三档. 冷却 `(code, PushKind)` 键, 走 `cooldown_secs()`. 日预算: 交易建议类合计 ≤ 30 条/日 (DAILY_BUDGET_LIMIT), 超预算转 log. Frozen/Unsafe 停发 T-03/05/07/10, 风险类 T-01/02/04/09 照发. 设计见 `v12-push-templates.md` §14.3 + `v12-push-uncertainty-notes.md`. **先登记后实现**: 代码已落地 (`src/bin/monitor/notify.rs` PushKind + `src/bin/monitor/push_templates.rs` 模板 + 治理). 待 PR1~4 接入调用点. | `src/bin/monitor/notify.rs::PushKind` + `src/bin/monitor/push_templates.rs` (45 单测已 PASS) | `cargo test --bin monitor push_templates` 45/45 PASS | 2026-07-05 |
| BR-026 | 分类/聚类 | v13 §14.1 P-01 盘前新闻聚类口径。`news_monitor_loop` cluster 输出 3 主线 (theme_1/2/3) + 2 催化 (news_1/2 + chain_1/2) + N 关注票。缺失字段整段省略（AGENTS §2.2），不静默填空。设计见 `v13-push-templates.md` §14.1 P-01。**先登记后实现 (AGENTS §2.10)**: PR #1 落地。| `src/bin/monitor/push_templates.rs::render_preopen_news_hot` | `src/bin/monitor/push_templates.rs` | 2026-07-06 |
| BR-027 | 映射 | v13 §14.2 I-02 新闻催化映射。`headline` + `theme` + 上涨个股 `(name, code, chg, reason)`。chg 缺失整行省略。设计见 `v13-push-templates.md` §14.2 I-02。**先登记后实现**: PR #1 落地。| `src/bin/monitor/push_templates.rs::render_news_catalyst` | `src/bin/monitor/push_templates.rs` | 2026-07-06 |
| BR-028 | 分类 | v13 §14.3 A-10 题材阶段判定。`persistent ∈ {High, Med, Low}` 持续性 + `started_names/pending_names` 启动状态。设计见 `v13-push-templates.md` §14.3 A-10。**先登记后实现**: PR #3 落地。| `src/bin/monitor/push_templates.rs::render_catalyst_review` | `src/bin/monitor/push_templates.rs` | 2026-07-06 |
| BR-029 | 分类/推荐 | v13 §14.4 D-01 新闻驱动个股。`theme/stage ∈ {Starting, Fermenting, Diverging}` + `action ∈ {Observe, BuyDip, DoNotChase}`。reason 缺失整行省略，action 缺失整段省略。设计见 `v13-push-templates.md` §14.4 D-01。**先登记后实现**: PR #2 落地。| `src/bin/monitor/push_templates.rs::render_news_to_idea` | `src/bin/monitor/push_templates.rs` | 2026-07-06 |
| BR-030 | 限额/窗口 | v13.1 §5.2 盘后固定价格申报窗口按交易所区分。**沪市 A 股/ETF**: 9:30-11:30, 13:00-15:30。**深市 A 股/ETF + 北交所 A 股**: 9:15-11:30, 13:00-15:30。撮合时间 15:05-15:30 全部统一。设计见 `v13-push-templates.md` §14.5 v13.1。**先登记后实现**: PR #6 落地。`market_open` 校验，窗口外返回 warning。| `src/bin/monitor/push_templates.rs::render_post_fixed_price_order` + `src/bin/monitor/push_templates.rs::render_post_fixed_price_fill` | `src/bin/monitor/push_templates.rs` | 2026-07-06 |
| BR-031 | 限额/阈值 | v13.1 §5.4 ST/*ST 涨跌幅变更 5%→10%（新规 2026-07-06）。所有 ST/*ST 持仓止损/止盈阈值从 5% 重算为 10%。T-16 推送含 `new_stop_loss/new_take_profit` 重算结果。config `st_price_limit.st_daily_limit = 0.10`。设计见 `v13-push-templates.md` §5.4。**先登记后实现**: PR #7 落地。helper: `src/bin/monitor/risk/st_recalc.rs::recalc_st_position`。| `src/bin/monitor/push_templates.rs::render_st_price_limit_changed` | `src/bin/monitor/push_templates.rs` | 2026-07-06 |
| BR-032 | 限额/分类 | v13.1 §5.5 上交所基金收盘集合竞价。14:57-15:00 集合竞价形成收盘价（原连续竞价）。仅沪市 ETF 适用。流动性 "抑制尾盘操纵"。设计见 `v13-push-templates.md` §5.5。**先登记后实现**: PR #9 落地。作用域过滤 `exchange == SH && instrument_type == ETF`。| `src/bin/monitor/push_templates.rs::render_etf_closing_call_auction` | `src/bin/monitor/push_templates.rs` | 2026-07-06 |
| BR-033 | 隔离/确认 | v13.1 §5.6 创业板协议大宗盘中实时确认（原"盘后统一确认"）。`real_time_confirm = true` 时立即推送。清算节奏: `SettleType ∈ {NextSession, RealTime}`。设计见 `v13-push-templates.md` §5.6。**先登记后实现**: PR #10 落地。| `src/bin/monitor/push_templates.rs::render_block_trade_intraday_confirm` + `src/bin/monitor/push_templates.rs::dispatch_block_trade_intraday_confirm` | `src/bin/monitor/push_templates.rs` | 2026-07-06 |
| BR-034 | 限额/区间 | v13.1 §5.7 北交所大宗价格范围。前收盘价 → 当日竞价交易实时成交均价。`block_price_range` 基于当日均价 ± N%。设计见 `v13-push-templates.md` §5.7。**先登记后实现**: PR #10 落地。作用域过滤 `exchange == BJ && no_price_limit == true`。| `src/bin/monitor/push_templates.rs::render_block_trade_price_range` + `src/bin/monitor/push_templates.rs::dispatch_block_trade_price_range` | `src/bin/monitor/push_templates.rs` | 2026-07-06 |
| BR-035 | 限额/校准 | v13.1 创业板做市商流动性阈值校准。引入做市商制度后，中小市值股票流动性增强。`config/strategy.toml::gem_market_maker.liquidity_threshold = 0.65`（原 0.50 + 0.15 校准）。影响 `position_tracker` 中小盘建仓门槛。设计见 `v13-push-templates.md` §5.8。**先登记后实现**: 治理参数已同步（commit `776444a`），代码实现待 v13 PR 接入。| 已落地: `config/strategy.toml` (commit `776444a`) | 待补（v13 PR 接入后）| 2026-07-06 |
| BR-036 | 过滤 | v14.1 主题搜索能力过滤：`search_topic` 仅纳入 `supports_topic_search() = true` 的 provider；`sse_szse/cninfo/kcb_daily` 不进入主题池；雪球无 `XUEQIU_COOKIE` 时 `is_available=false`；结构错配/空结果/死 lid 失败日志降级。 | `src/search_service/types.rs` + `src/search_service/service.rs::search_topic` + `src/search_service/providers/xueqiu.rs` + `src/search_service/providers/sina_flash.rs` | `cargo build --release --bin monitor` + 盘中日志噪声回归 | 2026-07-11 |
| BR-037 | 限额 | v14.1 CLS 签名与快讯池配额：CLS/KCB 请求统一走 `build_signed_params`，算法为 `SHA1_hex(MD5_hex(querystring))`；KCB 只进 `fetch_flash_titles`（不进主题/个股搜索池），并与 CLS 同源错峰 1s 抓取，失败显式报错不降级假数据。 | `src/search_service/providers/cls_sign.rs` + `src/search_service/providers/cls.rs` + `src/search_service/providers/kcb_daily.rs` + `src/search_service/service.rs::fetch_flash_titles` | `cargo test -p stock_analysis cls_sign` + monitor 实拉日志断言 | 2026-07-11 |
