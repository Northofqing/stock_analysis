# v29 D-01 Dispatcher 接入 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 v12 §14.4 D-01 (新闻驱动个股) dispatcher 接入 `news_monitor_loop` 实盘路径, 闭环"新闻 → 板块 → 个股"。

**Architecture:** 事件驱动模型。`news_monitor_loop` 每 2 min 拉到新公告时, 调 `dispatch_news_to_idea_daily` 推送 D-01。dispatcher 内部 1h/票 memo (静态 Mutex<HashMap>) + `push_governor` 20min 冷却, 双重去重。

**Tech Stack:** Rust 2021, tokio, once_cell v1.19 (已在 Cargo.toml), std::sync::Mutex, chrono, std::time::Instant。

## Global Constraints

- v12 §14.4 D-01 模板定义 (AGENTS.md §14.4)
- v12 §14.5 PushKind 治理 (NewsToIdea 20min 冷却, ⚡重要)
- 横幅 §14.0.1 强制 (即使写死 DataMode::Full)
- 数据真实性: 真实数据源, 无 mock (load_news_to_idea_snapshot_real 已在用真实候选台)
- 冷却防护: 至少 1 层去重 (本计划 2 层: memo + push_governor)
- 静默失败: 快照空时 log + 短路, 不刷屏
- 测试基线: 893/893 lib tests, 新增 1 个 memo 测试 → 894
- PR 最小: 严格 2 文件 (push_templates.rs + main.rs), 不动无关代码
- commit 命名: `feat(v29): D-01 dispatcher 接入 news_monitor_loop`
- 依赖: once_cell = "1.19" 已在 Cargo.toml, 无需新增
- BannerCtx 构造参照 `run_daily_pushes()` line 407-413 模式
- 横幅 DataMode 写死 Full (与现有 11 个已推模板一致)

---

## File Structure

| 文件 | 责任 | 改动 |
|---|---|---|
| `src/bin/monitor/push_templates.rs` | D-01 模板 + dispatcher 业务逻辑 | +30 行 (memo static + 检查 + 测试) |
| `src/bin/monitor/main.rs` | news_monitor_loop 实盘调度 | +18 行 (触发块) |
| `Cargo.toml` | 依赖 | 0 (once_cell 已有) |
| `docs/superpowers/specs/2026-07-07-d01-dispatcher-design.md` | 设计文档 (已存在) | 0 |

---

### Task 1: dispatcher 内部 memo 静态变量 (TDD: 先写测试)

**Files:**
- Modify: `src/bin/monitor/push_templates.rs:2120` (dispatch_news_to_idea_daily 函数体前)
- Test: `src/bin/monitor/push_templates.rs` (tests 模块末尾)

**Interfaces:**
- Consumes: std::time::Instant, std::collections::HashMap, std::sync::Mutex, once_cell::sync::Lazy
- Produces: `static D01_LAST_PUSH: Lazy<Mutex<HashMap<String, Instant>>>` (静态 memo 容器)

- [ ] **Step 1: 写失败的测试**

在 `src/bin/monitor/push_templates.rs` 的 `#[cfg(test)] mod tests` 末尾加:

```rust
    // v29: D-01 dispatcher memo 测试
    // 注: 验证 memo 容器可写入 + 可重置, 集成测试由 monitor --test --v13-diag 覆盖
    #[test]
    fn test_d01_memo_map_basic() {
        use super::{D01_LAST_PUSH, _reset_d01_memo_for_test};
        _reset_d01_memo_for_test();

        // 写入
        {
            let mut map = D01_LAST_PUSH.lock().unwrap();
            map.insert("000001:平安银行".to_string(), std::time::Instant::now());
        }

        // 读出
        let map = D01_LAST_PUSH.lock().unwrap();
        assert!(
            map.contains_key("000001:平安银行"),
            "memo 容器应包含刚插入的 key"
        );

        // 重置
        drop(map);
        _reset_d01_memo_for_test();
        let map = D01_LAST_PUSH.lock().unwrap();
        assert!(map.is_empty(), "重置后 memo 容器应为空");
    }
```

- [ ] **Step 2: 运行测试, 验证失败**

Run: `cargo test --lib push_templates::tests::test_d01_memo_map_basic`
Expected: FAIL with "cannot find type `D01_LAST_PUSH`" 或 "cannot find function `_reset_d01_memo_for_test`"

- [ ] **Step 3: 实现最小代码 (memo 容器)**

在 `src/bin/monitor/push_templates.rs` 第 2120 行 (dispatch_news_to_idea_daily 函数定义前) 加:

```rust
// v29: D-01 dispatcher 内部 memo (1h/票, 跨日重置)
// 静态 Lazy 容器, 跨函数调用复用
use std::sync::Mutex;
use std::time::Instant;
use std::collections::HashMap;
use once_cell::sync::Lazy;

pub static D01_LAST_PUSH: Lazy<Mutex<HashMap<String, Instant>>> =
    Lazy::new(|| Mutex::new(HashMap::new()));

/// v29: 测试用 - 重置 memo 容器
#[cfg(test)]
pub fn _reset_d01_memo_for_test() {
    D01_LAST_PUSH.lock().unwrap().clear();
}
```

⚠️ **注意**: 如果 `push_templates.rs` 顶部已有 `use std::sync::Mutex;` 等 import, 删掉重复的 use 语句 (避免 unused import 警告)。

- [ ] **Step 4: 跑测试, 验证通过**

Run: `cargo test --lib push_templates::tests::test_d01_memo_map_basic`
Expected: PASS (1 passed)

- [ ] **Step 5: 跑全量测试, 验证无回归**

Run: `cargo test --lib`
Expected: 894 passed (893 + 1 new test)

- [ ] **Step 6: 提交**

```bash
git add src/bin/monitor/push_templates.rs
git commit -m "feat(v29.1): D-01 dispatcher memo 静态容器 + 单元测试"
```

---

### Task 2: dispatch_news_to_idea_daily 加 memo 检查

**Files:**
- Modify: `src/bin/monitor/push_templates.rs:2121-2133` (dispatch_news_to_idea_daily 函数体)

**Interfaces:**
- Consumes: Task 1 的 `D01_LAST_PUSH` 静态容器, `load_news_to_idea_snapshot_real` 返回的 `NewsToIdeaSnapshot` 的 `code` 和 `name` 字段
- Produces: 修改后的 `dispatch_news_to_idea_daily`, 命中 memo 短路返回 false, log 写明冷却剩余秒数

- [ ] **Step 1: 修改函数体, 加 memo 检查**

把 `src/bin/monitor/push_templates.rs:2121-2133` 的函数体:

```rust
/// v15.5 业务层入口 (v16.4 改用真实候选台数据)
pub async fn dispatch_news_to_idea_daily(hhmm: &str, banner: &BannerCtx) -> bool {
    let snapshot = load_news_to_idea_snapshot_real(hhmm);
    if snapshot.headline.is_empty() {
        log_dispatcher_attempt("D-01", false, 0, "news_to_idea_snapshot empty");
        log::info!("[D-01] news_to_idea_snapshot 空 (候选台无候选), 跳过推送");
        return false;
    }
    let params = build_news_to_idea_from_snapshot(&snapshot);
    let snap_size = snapshot.reasons.len();
    let result = push_news_to_idea("", Some(banner), params).await;
    log_dispatcher_attempt("D-01", result, snap_size, "");
    result
}
```

替换为:

```rust
/// v15.5 业务层入口 (v16.4 改用真实候选台数据)
/// v29: 加 dispatcher 内部 memo (1h/票) — 防止公告密集时同票刷屏
pub async fn dispatch_news_to_idea_daily(hhmm: &str, banner: &BannerCtx) -> bool {
    let snapshot = load_news_to_idea_snapshot_real(hhmm);
    if snapshot.headline.is_empty() {
        log_dispatcher_attempt("D-01", false, 0, "news_to_idea_snapshot empty");
        log::info!("[D-01] news_to_idea_snapshot 空 (候选台无候选), 跳过推送");
        return false;
    }

    // v29: memo 1h/票 (与 push_governor 20min 冷却叠加, 实际间隔 ≥ 1h)
    let memo_key = format!("{}:{}", snapshot.code, snapshot.name);
    {
        let mut map = D01_LAST_PUSH.lock().unwrap();
        if let Some(last) = map.get(&memo_key) {
            let elapsed = last.elapsed().as_secs();
            if elapsed < 3600 {
                log_dispatcher_attempt(
                    "D-01",
                    false,
                    0,
                    &format!("1h memo 冷却, 还需 {}s", 3600 - elapsed),
                );
                log::info!(
                    "[D-01] {}:{} memo 冷却中, 跳过推送 (剩余 {}s)",
                    snapshot.code,
                    snapshot.name,
                    3600 - elapsed
                );
                return false;
            }
        }
        map.insert(memo_key.clone(), Instant::now());
    }

    let params = build_news_to_idea_from_snapshot(&snapshot);
    let snap_size = snapshot.reasons.len();
    let result = push_news_to_idea("", Some(banner), params).await;
    log_dispatcher_attempt("D-01", result, snap_size, "");
    result
}
```

- [ ] **Step 2: 跑全量测试, 验证无回归**

Run: `cargo test --lib`
Expected: 894 passed (memo 短路路径不影响现有测试, 因 snapshot 为空时在前一行已 return)

- [ ] **Step 3: 提交**

```bash
git add src/bin/monitor/push_templates.rs
git commit -m "feat(v29.2): dispatch_news_to_idea_daily 加 memo 1h/票 检查"
```

---

### Task 3: news_monitor_loop 加 D-01 触发块

**Files:**
- Modify: `src/bin/monitor/main.rs:2773` (NewsRanked push_governor 调用之后, line 2775 之前)

**Interfaces:**
- Consumes: Task 1/2 的 `dispatch_news_to_idea_daily`, push_templates::BannerCtx
- Produces: news_monitor_loop 每轮 (2 min) 当 `!pushed.is_empty()` 时, 触发 D-01 dispatcher

- [ ] **Step 1: 插入 D-01 触发块**

把 `src/bin/monitor/main.rs:2770-2775`:

```rust
                // v19.12: NewsRanker 真推 (无 A 档门槛, 用户要求全推)
                let board = news_ranker::format_news_ranked_board(&ranked);
                log::info!("[NewsRanker 盘中] {} 条公告 → 真推飞书", news_anns.len());
                notify::push_governor(&board, notify::PushKind::NewsRanked).await;
            }
        }
```

替换为:

```rust
                // v19.12: NewsRanker 真推 (无 A 档门槛, 用户要求全推)
                let board = news_ranker::format_news_ranked_board(&ranked);
                log::info!("[NewsRanker 盘中] {} 条公告 → 真推飞书", news_anns.len());
                notify::push_governor(&board, notify::PushKind::NewsRanked).await;
            }
        }

        // ═══════════════════════════════════════════════════════════════
        // v29: D-01 新闻驱动个股推送 (事件驱动)
        //   - 触发: pushed 不空 (有重要公告/事件) 时, 每轮 news_monitor_loop 调一次
        //   - 去重: dispatcher memo 1h/票 + push_governor 20min 冷却 (v12 §14.5)
        //   - 数据源: 候选台 (5 源合并) - 与 NewsRanked 公告影子 rank 互补
        //   - 静默: 候选台空时短路返回, log
        // ═══════════════════════════════════════════════════════════════
        if !pushed.is_empty() {
            use push_templates::dispatch_news_to_idea_daily;
            let banner = push_templates::BannerCtx {
                account_mode: push_templates::AccountMode::Normal,
                total_pos: 0,
                today_pnl: 0.0,
                data_mode: push_templates::DataMode::Full,
                data_missing_note: None,
            };
            let now_ts = chrono::Local::now();
            let hhmm = now_ts.format("%H:%M").to_string();
            let _ = dispatch_news_to_idea_daily(&hhmm, &banner).await;
        }
```

- [ ] **Step 2: 编译验证**

Run: `cargo build --lib`
Expected: Finished `dev` profile, no errors

- [ ] **Step 3: 跑全量测试, 验证无回归**

Run: `cargo test --lib`
Expected: 894 passed (不新增, 不减少)

- [ ] **Step 4: 跑 monitor binary build**

Run: `cargo build --bin monitor`
Expected: Finished, no errors

- [ ] **Step 5: 提交**

```bash
git add src/bin/monitor/main.rs
git commit -m "feat(v29.3): news_monitor_loop 触发 D-01 dispatcher (事件驱动)"
```

---

### Task 4: 端到端验证

**Files:**
- Modify: 0 (验证任务, 不改代码)

**Interfaces:**
- Consumes: Task 1/2/3 的全部产出
- Produces: cargo test + v13.27 端到端诊断 + commit 验证

- [ ] **Step 1: 全量测试**

Run: `cargo test --lib --no-fail-fast 2>&1 | tail -5`
Expected: `test result: ok. 894 passed; 0 failed; 2 ignored`

- [ ] **Step 2: 编译 release 验证**

Run: `cargo build --release --bin monitor 2>&1 | tail -5`
Expected: `Finished \`release\` profile [optimized] target(s)`, no errors

- [ ] **Step 3: 干跑 v13.27 端到端诊断 (沙箱无网络, 5 个 dispatcher 全空, 正常)**

Run: `cargo run --bin monitor -- --test --v13-diag 2>&1 | tail -15`
Expected:
```
=== v13.27 端到端诊断 ===
生成时间: ...
总步骤: 5, OK: 0, 空: 5, 错: 0
  ○ D-01 load_news_to_idea (XXms) empty
...
报告已写入: data/v13_diag_report.json
```
(沙箱无网络, load_news_to_idea_snapshot_real 返回 default → empty, 正常)

- [ ] **Step 4: 验证 git log**

Run: `git log --oneline -5`
Expected:
```
xxxxxxx feat(v29.3): news_monitor_loop 触发 D-01 dispatcher (事件驱动)
yyyyyyy feat(v29.2): dispatch_news_to_idea_daily 加 memo 1h/票 检查
zzzzzzz feat(v29.1): D-01 dispatcher memo 静态容器 + 单元测试
978dd83 docs(v29 design): D-01 dispatcher 接入 news_monitor_loop
026893a fix(v28): 雪球重新启用 (cookie) + v24 BR-006 旧测试清理
```

- [ ] **Step 5: 验证未提交修改为空**

Run: `git status --short`
Expected: 空 (无 uncommitted 改动)

---

### Task 5: 实现日志 (impl log)

**Files:**
- Modify: `docs/superpowers/specs/2026-07-07-d01-dispatcher-design.md` (末尾追加实现日志)

**Interfaces:**
- Consumes: Task 1-4 的所有 commit hash
- Produces: 简短实现日志, 记录实际行数 + 测试数 + 关键决策

- [ ] **Step 1: 追加实现日志**

在 spec 文件末尾 (`## 10. 参考` 后) 追加:

```markdown

---

## 11. 实现日志 (2026-07-07)

**实际改动**:
- `src/bin/monitor/push_templates.rs`: +35 行 (memo static + 检查 + 测试)
- `src/bin/monitor/main.rs`: +18 行 (news_monitor_loop 触发块)
- 合计: +53 行, 2 文件

**测试**: 893 → 894 (+1 memo 容器测试)

**Commits**:
1. `xxxxxxx` feat(v29.1): D-01 dispatcher memo 静态容器 + 单元测试
2. `yyyyyyy` feat(v29.2): dispatch_news_to_idea_daily 加 memo 1h/票 检查
3. `zzzzzzz` feat(v29.3): news_monitor_loop 触发 D-01 dispatcher (事件驱动)

**未做** (后续 PR):
- 盘前 P-01~P-04 / 盘中 I-01~I-04 / 盘后 A-10 模板接入
- `run_daily_pushes()` 未被调用 (--push 模式完全失效)
- AccountMode 评估接横幅 (DataMode 写死 Full)
```

- [ ] **Step 2: 提交日志**

```bash
git add -f docs/superpowers/specs/2026-07-07-d01-dispatcher-design.md
git commit -m "docs(v29 impl log): D-01 dispatcher 接入完成 (894 tests, 53 行)"
```

---

## Self-Review

### 1. Spec coverage

| Spec 章节 | 任务 | 状态 |
|---|---|---|
| §2.1 调用链 (news_monitor_loop 触发) | Task 3 | ✓ |
| §2.3 触发模型 (事件驱动) | Task 3 (if !pushed.is_empty()) | ✓ |
| §3.1 dispatcher 内部 memo | Task 1 + Task 2 | ✓ |
| §3.2 news_monitor_loop 触发块 | Task 3 | ✓ |
| §4.1 memo 放 dispatcher 内部 (静态) | Task 1 (Lazy<Mutex<HashMap>>) | ✓ |
| §4.2 事件驱动 (用户选择) | Task 3 | ✓ |
| §4.3 与 NewsRanked 不重复 | Task 3 (在 NewsRanked 之后) | ✓ |
| §5.1 单元测试 (memo 容器) | Task 1 Step 1 | ✓ |
| §5.2 集成测试 (v13.27 diag) | Task 4 Step 3 | ✓ |
| §6 风险: once_cell 依赖 | Global Constraints (已声明) | ✓ |
| §6 风险: data_mode 写死 Full | Task 3 (BannerCtx 构造) | ✓ |
| §7.1 涉及文件 | Task 1, 2, 3 (2 文件) | ✓ |
| §9 AC1 (893 → 894) | Task 1 + Task 4 | ✓ |
| §9 AC2 (编译通过) | Task 3 + Task 4 | ✓ |
| §9 AC3 (v13.27 diag) | Task 4 | ✓ |
| §9 AC4 (实盘 1 日) | 文档化 (用户跑) | ✓ |
| §9 AC5 (memo 生效) | Task 1 测试 + Task 2 检查 | ✓ |
| §9 AC6 (snapshot 空短路) | Task 2 (现有逻辑保留) | ✓ |

### 2. Placeholder scan

| 模式 | 扫描结果 |
|---|---|
| TBD / TODO / TBA | 0 命中 |
| "implement later" / "fill in details" | 0 命中 |
| "add appropriate error handling" | 0 命中 (memo 检查覆盖冷却逻辑) |
| "Similar to Task N" | 0 命中 (BannerCtx 构造完整复刻 run_daily_pushes line 407-413) |
| 类型/函数引用未定义 | 0 命中 (D01_LAST_PUSH / _reset_d01_memo_for_test 在 Task 1 定义, Task 2/3 引用) |

### 3. Type consistency

| Task | 引用 | 定义 |
|---|---|---|
| Task 1 | `D01_LAST_PUSH: Lazy<Mutex<HashMap<String, Instant>>>` | Task 1 Step 3 ✓ |
| Task 1 | `_reset_d01_memo_for_test()` | Task 1 Step 3 ✓ |
| Task 2 | `D01_LAST_PUSH.lock().unwrap()` | Task 1 ✓ |
| Task 2 | `Instant::now()` (std::time) | Task 1 use ✓ |
| Task 3 | `push_templates::dispatch_news_to_idea_daily` | Task 2 (改动函数体, 签名不变) ✓ |
| Task 3 | `push_templates::BannerCtx / AccountMode / DataMode` | main.rs 既有 (line 407-413) ✓ |

### Self-Review 结论

✓ Spec 全部 17 个要求覆盖  
✓ 0 占位符  
✓ 类型/函数引用一致  
✓ TDD (Task 1 先写测试, 后写实现)  
✓ 频繁 commit (3 个 feat commit + 1 docs commit)  
✓ PR 最小 (2 文件)  
✓ 测试数 893 → 894 (1 新增)

---

## Execution Handoff

**Plan complete and saved to `docs/superpowers/plans/2026-07-07-v29-d01-dispatcher.md`.**

任务清单:
1. Task 1: D-01 memo 静态容器 + 单元测试 (TDD: 先写测试后写实现)
2. Task 2: dispatch_news_to_idea_daily 加 memo 检查
3. Task 3: news_monitor_loop 触发块
4. Task 4: 端到端验证 (cargo test / build / v13.27 diag)
5. Task 5: 实现日志 (impl log)

预计耗时: ~30 min (3 commit + 1 docs commit + 1 verification)
预计代码量: +53 行, 2 文件
预计测试: 893 → 894

执行选项 (按 superpowers:subagent-driven-development 或 superpowers:executing-plans 风格):

**1. Subagent-Driven (推荐)** - 我每个 Task 派一个 fresh subagent, 任务间有 review gate, 快速迭代

**2. Inline Execution** - 在当前 session 串行执行, 批量 commit + checkpoint review

选哪种?
