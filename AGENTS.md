# 仓库协作规则

## 按改动范围验证

- 先判断改动影响，完成一组相关修改后，运行能验证该改动的最小充分检查。验证范围随行为风险扩大，不因修改次数增加。
- 仅修改文档、注释或不影响行为的文案：检查内容和 `git diff --check`，无需运行测试或编译。
- 局部逻辑修改：优先运行相关测试，并限定 Cargo target，例如 `cargo test --lib <过滤条件>` 或 `cargo test --test <测试目标> <过滤条件>`，避免仅加过滤词却编译全部测试目标。
- 仅需确认类型或编译正确性时，运行针对目标的 `cargo check`。相关测试已经通过后，复用其编译结果作为证据，无需为同一目标再例行追加 `check`、`build`、`clippy`。
- 新增测试应验证有意义的行为、缺陷回归或必要边界；简单、可逆、低风险修改不默认补测试，也不编写仅重复实现细节的用例。用户要求 TDD 或缺陷需要失败示例时，再使用先失败后修复的流程。
- 公共接口、依赖、跨模块逻辑或交易、风控、持久投递等关键行为变化时，根据受影响路径扩大验证；有明确覆盖需要、CI 要求或用户要求时，才运行全量测试。
- `release` 构建用于上线任务或确需 release 产物的验证。activation 重发和 monitor 重启按部署任务需要执行，日常源码编辑本身不触发部署流程；上线时仍遵守相应激活要求。
- 检查通过后即停止验证。只有新的相关修改、检查失败或尚未解决的具体疑点，才追加或重复检查；无需为同一份改动反复跑相同命令。
- 交付时简要说明实际验证范围及未覆盖的重要部分，结论与已有证据一致。

## 项目上下文

需要架构、运行配置或上线信息时，阅读 [CLAUDE.md](CLAUDE.md)。其中的命令是可用操作，接线与上线流程按任务范围执行。
