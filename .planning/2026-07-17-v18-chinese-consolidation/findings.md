# 发现

- `docs/v18.x` 当前有 1 个英文索引和 4 份来源文档。
- 来源覆盖：平台评估、总体架构、四核心模块详细接口、实施路线图。
- 两份来源主体为英文，一份主体为中文，一份英文路线图；存在明显重复的 Gate、旧模块关系、回滚与验收内容。
- 工作树已有大量用户改动，必须限定路径操作并避免任何清理命令。
- `CLAUDE.md` 要求文档中的代码事实可复现、禁止未经证据的“完成/100%/收官”声明。
- 本次只做文档整合；数据红线 2.1–2.10 应作为设计约束保留，不引入新阈值或业务规则。
- 建议的信息依赖顺序为：现状/问题 → v18 边界 → 目标链路 → 领域契约与四模块 → 失败模式/旧模块 → 实施流 → Gate/验收/回滚。
- 评估结论：当前是可信的事件驱动监控与模拟交易基础，但尚不是可审计的研究到实盘系统；v18 应先闭合机构级模拟交易链路，实盘执行另设 Gate L。
- 核心 P0：错误转默认值、合成健康状态、行情时效口径偏离 5 秒、缺少经验证的券商订单/成交/对账生命周期。
- 权威关联键为不可变 `decision_id`，端到端传播 `correlation_id`；通知只能消费决策，不代表成交。
- 审计设计要求 WORM/Object-Lock 至少五年，SQLite 仅为查询投影；哈希链或 append-only SQLite 本身不满足 2.7。
- 四模块实施依赖严格为：Data Contract Gate → DecisionRecord/CandidateSnapshot → Paper Order/Fill/Ledger → Attribution/ModelChangeProposal。
- `docs/` 与 `.planning/` 的新文件会被 `.gitignore` 覆盖；中文整合内容应写入已跟踪的 v18 活跃总体设计文件。
